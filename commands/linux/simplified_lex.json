{
  "command": "lex",
  "description": "Lexical analyzer generator. Given the specification for a lexical analyzer, generates C code implementing it. More information: https://manned.org/lex.1.",
  "useCases": [
    {
      "description": "Generate an analyzer from a Lex file, storing it to the file lex.yy.c:",
      "syntax": "lex {{analyzer.l}}"
    },
    {
      "description": "Write analyzer to stdout:",
      "syntax": "lex -{{-stdout|t}} {{analyzer.l}}"
    },
    {
      "description": "Specify the output file:",
      "syntax": "lex {{analyzer.l}} --outfile {{analyzer.c}}"
    },
    {
      "description": "Generate a [B]atch scanner instead of an interactive scanner:",
      "syntax": "lex -B {{analyzer.l}}"
    },
    {
      "description": "Compile a C file generated by Lex:",
      "syntax": "cc {{path/to/lex.yy.c}} --output {{executable}}"
    }
  ]
}