{
  "command": "distrobox-enter",
  "description": "Enter a Distrobox container. See also: tldr distrobox. Default command executed is your SHELL, but you can specify different shells or entire commands to execute. If used inside a script, an application, or a service, you can use the --headless mode to disable the tty and interactivity. More information: https://distrobox.it/usage/distrobox-enter.",
  "useCases": [
    {
      "description": "Enter a Distrobox container:",
      "syntax": "distrobox-enter {{container_name}}"
    },
    {
      "description": "Enter a Distrobox container and run a command at login:",
      "syntax": "distrobox-enter {{container_name}} -- {{sh -l}}"
    },
    {
      "description": "Enter a Distrobox container without instantiating a tty:",
      "syntax": "distrobox-enter --name {{container_name}} -- {{uptime -p}}"
    }
  ]
}