{
  "command": "cat",
  "description": "Print and concatenate files. More information: https://www.gnu.org/software/coreutils/manual/html_node/cat-invocation.html.",
  "useCases": [
    {
      "description": "Print the contents of a file to stdout:",
      "syntax": "cat {{path/to/file}}"
    },
    {
      "description": "Concatenate several files into an output file:",
      "syntax": "cat {{path/to/file1 path/to/file2 ...}} &gt; {{path/to/output_file}}"
    },
    {
      "description": "Append several files to an output file:",
      "syntax": "cat {{path/to/file1 path/to/file2 ...}} &gt;&gt; {{path/to/output_file}}"
    },
    {
      "description": "Write stdin to a file:",
      "syntax": "cat - &gt; {{path/to/file}}"
    },
    {
      "description": "[n]umber all output lines:",
      "syntax": "cat -n {{path/to/file}}"
    },
    {
      "description": "Display non-printable and whitespace characters (with M- prefix if non-ASCII):",
      "syntax": "cat -v -t -e {{path/to/file}}"
    }
  ]
}