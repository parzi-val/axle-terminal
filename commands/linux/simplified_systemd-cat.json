{
  "command": "systemd-cat",
  "description": "Connect a pipeline or program's output streams with the systemd journal. More information: https://www.freedesktop.org/software/systemd/man/systemd-cat.html.",
  "useCases": [
    {
      "description": "Write the output of the specified command to the journal (both output streams are captured):",
      "syntax": "systemd-cat {{command}}"
    },
    {
      "description": "Write the output of a pipeline to the journal (stderr stays connected to the terminal):",
      "syntax": "{{command}} | systemd-cat"
    }
  ]
}