{
  "command": "lxi",
  "description": "Control LXI compatible instruments such as oscilloscopes. More information: https://github.com/lxi-tools/lxi-tools.",
  "useCases": [
    {
      "description": "Discover LXI devices on available networks:",
      "syntax": "lxi discover"
    },
    {
      "description": "Capture a screenshot, detecting a plugin automatically:",
      "syntax": "lxi screenshot --address {{ip_address}}"
    },
    {
      "description": "Capture a screenshot using a specified plugin:",
      "syntax": "lxi screenshot --address {{ip_address}} --plugin {{rigol-1000z}}"
    },
    {
      "description": "Send an SCPI command to an instrument:",
      "syntax": "lxi scpi --address {{ip_address}} &quot;{{*IDN?}}&quot;"
    },
    {
      "description": "Run a benchmark for request and response performance:",
      "syntax": "lxi benchmark --address {{ip_address}}"
    }
  ]
}