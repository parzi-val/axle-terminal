{
  "command": "avahi-resolve",
  "description": "Translate between host names and IP Addresses. More information: https://www.avahi.org/.",
  "useCases": [
    {
      "description": "Resolve a local service to its IPv4:",
      "syntax": "avahi-resolve -4 --name {{service.local}}"
    },
    {
      "description": "Resolve an IP to a hostname, verbosely:",
      "syntax": "avahi-resolve --verbose --address {{IP}}"
    }
  ]
}