{
  "command": "rc-update",
  "description": "Add and remove OpenRC services to and from runlevels. See also openrc. More information: https://manned.org/rc-update.",
  "useCases": [
    {
      "description": "List all services and the runlevels they are added to:",
      "syntax": "rc-update show"
    },
    {
      "description": "Add a service to a runlevel:",
      "syntax": "sudo rc-update add {{service_name}} {{runlevel}}"
    },
    {
      "description": "Delete a service from a runlevel:",
      "syntax": "sudo rc-update delete {{service_name}} {{runlevel}}"
    },
    {
      "description": "Delete a service from all runlevels:",
      "syntax": "sudo rc-update --all delete {{service_name}}"
    }
  ]
}