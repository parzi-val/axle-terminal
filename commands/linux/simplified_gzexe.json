{
  "command": "gzexe",
  "description": "Compress executable files while keeping them executable. Back up the original file, appending ~ to its name and create a shell script that uncompresses and executes the binary inside it. More information: https://manned.org/gzexe.1.",
  "useCases": [
    {
      "description": "Compress an executable file in-place:",
      "syntax": "gzexe {{path/to/executable}}"
    },
    {
      "description": "Decompress a compressed executable in-place (i.e. convert the shell script back to an uncompressed binary):",
      "syntax": "gzexe -d {{path/to/compressed_executable}}"
    }
  ]
}