{
  "command": "woeusb",
  "description": "Windows media creation tool. More information: https://github.com/WoeUSB/WoeUSB.",
  "useCases": [
    {
      "description": "Format a USB then create a bootable Windows installation drive:",
      "syntax": "woeusb --device {{path/to/windows.iso}} {{/dev/sdX}}"
    },
    {
      "description": "Copy Windows files to an existing partition of a USB storage device and make it bootable, without erasing the current data:",
      "syntax": "woeusb --partition {{path/to/windows.iso}} {{/dev/sdXN}}"
    }
  ]
}