{
  "command": "mkfs.vfat",
  "description": "Create an MS-DOS filesystem inside a partition. More information: https://manned.org/mkfs.vfat.",
  "useCases": [
    {
      "description": "Create a vfat filesystem inside partition 1 on device b (sdb1):",
      "syntax": "mkfs.vfat {{/dev/sdb1}}"
    },
    {
      "description": "Create filesystem with a volume-name:",
      "syntax": "mkfs.vfat -n {{volume_name}} {{/dev/sdb1}}"
    },
    {
      "description": "Create filesystem with a volume-id:",
      "syntax": "mkfs.vfat -i {{volume_id}} {{/dev/sdb1}}"
    },
    {
      "description": "Use 5 instead of 2 file allocation tables:",
      "syntax": "mkfs.vfat -f 5 {{/dev/sdb1}}"
    }
  ]
}