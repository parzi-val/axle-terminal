{
  "command": "mkfs.xfs",
  "description": "Create an XFS filesystem inside a partition. More information: https://manned.org/mkfs.xfs.",
  "useCases": [
    {
      "description": "Create an XFS filesystem inside partition 1 on a device (X):",
      "syntax": "sudo mkfs.xfs {{/dev/sdX1}}"
    },
    {
      "description": "Create an XFS filesystem with a volume label:",
      "syntax": "sudo mkfs.xfs -L {{volume_label}} {{/dev/sdX1}}"
    }
  ]
}