{
  "command": "pkgctl release",
  "description": "Release step to commit, tag and upload build artifacts. More information: https://manned.org/pkgctl-release.1.",
  "useCases": [
    {
      "description": "Release a build artifact:",
      "syntax": "pkgctl release --repo {{repository}} --message {{commit_message}}"
    }
  ]
}