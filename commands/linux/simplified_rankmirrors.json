{
  "command": "rankmirrors",
  "description": "Rank a list of Pacman mirrors by connection and opening speed. Writes the new mirrorlist to stdout. More information: https://manned.org/rankmirrors.",
  "useCases": [
    {
      "description": "Rank a mirror list:",
      "syntax": "rankmirrors {{/etc/pacman.d/mirrorlist}}"
    },
    {
      "description": "Output only a given number of the top ranking servers:",
      "syntax": "rankmirrors -n {{number}} {{/etc/pacman.d/mirrorlist}}"
    },
    {
      "description": "Be verbose when generating the mirrorlist:",
      "syntax": "rankmirrors -v {{/etc/pacman.d/mirrorlist}}"
    },
    {
      "description": "Test only a specific URL:",
      "syntax": "rankmirrors --url {{url}}"
    },
    {
      "description": "Output only the response times instead of a full mirrorlist:",
      "syntax": "rankmirrors --times {{/etc/pacman.d/mirrorlist}}"
    }
  ]
}