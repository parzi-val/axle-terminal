{
  "command": "swapon",
  "description": "Enable devices and files for swapping. Note: path/to/file can either point to a regular file or a swap partition. More information: https://manned.org/swapon.",
  "useCases": [
    {
      "description": "Show swap information:",
      "syntax": "swapon"
    },
    {
      "description": "Enable a given swap area:",
      "syntax": "swapon {{path/to/file}}"
    },
    {
      "description": "Enable all swap areas specified in /etc/fstab except those with the noauto option:",
      "syntax": "swapon --all"
    },
    {
      "description": "Enable a swap partition by its label:",
      "syntax": "swapon -L {{label}}"
    }
  ]
}