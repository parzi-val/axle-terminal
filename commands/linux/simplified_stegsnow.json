{
  "command": "stegsnow",
  "description": "Steganography tool for concealing and extracting messages in text files encoded as tabs and spaces. More information: https://darkside.com.au/snow/manual.html.",
  "useCases": [
    {
      "description": "Extract [m]essage from file:",
      "syntax": "stegsnow {{path/to/file.txt}}"
    },
    {
      "description": "Extract [C]ompressed and [p]assword protected [m]essage from file:",
      "syntax": "stegsnow -C -p {{password}} {{path/to/file.txt}}"
    },
    {
      "description": "Determine approximate [S]torage capacity with line [l]ength less than 72 for file:",
      "syntax": "stegsnow -S -l 72 {{path/to/file.txt}}"
    },
    {
      "description": "Conceal [m]essage in text from file and save to result:",
      "syntax": "stegsnow -m '{{message}}' {{path/to/file.txt}} {{path/to/result.txt}}"
    },
    {
      "description": "Conceal message [f]ile content [C]ompressed in text from file and save to result:",
      "syntax": "stegsnow -C -f '{{path/to/message.txt}}' {{path/to/file.txt}} {{path/to/result.txt}}"
    },
    {
      "description": "Conceal [m]essage [C]ompressed and [p]assword protected in text from file and save to result:",
      "syntax": "stegsnow -C -p {{password}} -m '{{message}}' {{path/to/file.txt}} {{path/to/result.txt}}"
    }
  ]
}