{
  "command": "qm reset",
  "description": "Reset a virtual machine on QEMU/KVM Virtual Machine Manager. More information: https://pve.proxmox.com/pve-docs/qm.1.html.",
  "useCases": [
    {
      "description": "Reset a virtual machine:",
      "syntax": "qm reset {{vm_id}}"
    },
    {
      "description": "Reset a virtual machine and skip lock (only root can use this option):",
      "syntax": "qm reset --skiplock {{true}} {{vm_id}}"
    }
  ]
}