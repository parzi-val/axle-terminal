{
  "command": "mssh",
  "description": "GTK+ based SSH client for interacting with multiple SSH servers at once. More information: https://manned.org/mssh.",
  "useCases": [
    {
      "description": "Open a new window and connect to multiple SSH servers:",
      "syntax": "mssh {{user@host1}} {{user@host2}} {{...}}"
    },
    {
      "description": "Open a new window and connect to a group of servers predefined in ~/.mssh_clusters:",
      "syntax": "mssh --alias {{alias_name}}"
    }
  ]
}