{
  "command": "partx",
  "description": "Parse a partition table and tell the kernel about it. More information: https://manned.org/partx.",
  "useCases": [
    {
      "description": "List the partitions on a block device or disk image:",
      "syntax": "sudo partx --list {{path/to/device_or_disk_image}}"
    },
    {
      "description": "Add all the partitions found in a given block device to the kernel:",
      "syntax": "sudo partx --add --verbose {{path/to/device_or_disk_image}}"
    },
    {
      "description": "Delete all the partitions found from the kernel (does not alter partitions on disk):",
      "syntax": "sudo partx --delete {{path/to/device_or_disk_image}}"
    }
  ]
}