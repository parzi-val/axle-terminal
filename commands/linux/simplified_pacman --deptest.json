{
  "command": "pacman --deptest",
  "description": "Check each dependency specified and return a list of dependencies that are not currently satisfied on the system. See also: pacman. More information: https://manned.org/pacman.8.",
  "useCases": [
    {
      "description": "Print the package names of the dependencies that are not installed:",
      "syntax": "pacman -T {{package1 package2 ...}}"
    },
    {
      "description": "Check if the installed package satisfies the given minimum version:",
      "syntax": "pacman -T &quot;{{bash&gt;=5}}&quot;"
    },
    {
      "description": "Check if a later version of a package is installed:",
      "syntax": "pacman -T &quot;{{bash&gt;5}}&quot;"
    },
    {
      "description": "Display [h]elp:",
      "syntax": "pacman -Th"
    }
  ]
}