{
  "command": "repo-add",
  "description": "Package database maintenance utility which enables installation of said package via Pacman. More information: https://manned.org/repo-add.",
  "useCases": [
    {
      "description": "Create an empty repository:",
      "syntax": "repo-add {{path/to/database.db.tar.gz}}"
    },
    {
      "description": "Add all package binaries in the current directory and remove the old database file:",
      "syntax": "repo-add --remove {{path/to/database.db.tar.gz}} {{*.pkg.tar.zst}}"
    },
    {
      "description": "Add all package binaries in the current directory in silent mode except for warning and error messages:",
      "syntax": "repo-add --quiet {{path/to/database.db.tar.gz}} {{*.pkg.tar.zst}}"
    },
    {
      "description": "Add all package binaries in the current directory without showing color:",
      "syntax": "repo-add --nocolor {{path/to/database.db.tar.gz}} {{*.pkg.tar.zst}}"
    }
  ]
}