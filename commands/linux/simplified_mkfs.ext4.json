{
  "command": "mkfs.ext4",
  "description": "Create an ext4 filesystem inside a partition. More information: https://manned.org/mkfs.ext4.",
  "useCases": [
    {
      "description": "Create an ext4 filesystem inside partition 1 on device b (sdb1):",
      "syntax": "sudo mkfs.ext4 {{/dev/sdb1}}"
    },
    {
      "description": "Create an ext4 filesystem with a volume-label:",
      "syntax": "sudo mkfs.ext4 -L {{volume_label}} {{/dev/sdb1}}"
    }
  ]
}