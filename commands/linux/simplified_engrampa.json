{
  "command": "engrampa",
  "description": "Package files into zip/tar file in MATE desktop environment. See also: zip, tar. More information: https://github.com/mate-desktop/engrampa.",
  "useCases": [
    {
      "description": "Start Engrampa:",
      "syntax": "engrampa"
    },
    {
      "description": "Open specific archives:",
      "syntax": "engrampa {{path/to/archive1.tar path/to/archive2.tar ...}}"
    },
    {
      "description": "Archive specific files and/or directories recursively:",
      "syntax": "engrampa --add-to={{path/to/compressed.tar}} {{path/to/file_or_directory1 path/to/file_or_directory2 ...}}"
    },
    {
      "description": "Extract files and/or directories from archives to a specific path:",
      "syntax": "engrampa --extract-to={{path/to/directory}} {{path/to/archive1.tar path/to/archive2.tar ...}}"
    }
  ]
}