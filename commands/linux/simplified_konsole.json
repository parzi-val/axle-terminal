{
  "command": "konsole",
  "description": "KDE's terminal emulator. More information: https://docs.kde.org/stable5/en/konsole/konsole/command-line-options.html.",
  "useCases": [
    {
      "description": "Open the terminal in a specific directory:",
      "syntax": "konsole --workdir {{path/to/directory}}"
    },
    {
      "description": "[e]xecute a specific command and don't close the window after it exits:",
      "syntax": "konsole --noclose -e &quot;{{command}}&quot;"
    },
    {
      "description": "Open a new tab:",
      "syntax": "konsole --new-tab"
    },
    {
      "description": "Open the terminal in the background and bring to the front when Ctrl+Shift+F12 is pressed:",
      "syntax": "konsole --background-mode"
    }
  ]
}