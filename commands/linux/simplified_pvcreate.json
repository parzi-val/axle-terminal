{
  "command": "pvcreate",
  "description": "Initialize a disk or partition for use as a physical volume. See also: lvm. More information: https://manned.org/pvcreate.",
  "useCases": [
    {
      "description": "Initialize the /dev/sda1 volume for use by LVM:",
      "syntax": "pvcreate {{/dev/sda1}}"
    },
    {
      "description": "Force the creation without any confirmation prompts:",
      "syntax": "pvcreate --force {{/dev/sda1}}"
    }
  ]
}