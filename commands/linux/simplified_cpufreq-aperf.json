{
  "command": "cpufreq-aperf",
  "description": "Calculate the average CPU frequency over a time period. Requires root privileges. More information: https://manned.org/cpufreq-aperf.",
  "useCases": [
    {
      "description": "Start calculating, defaulting to all CPU cores and 1 second refresh interval:",
      "syntax": "sudo cpufreq-aperf"
    },
    {
      "description": "Start calculating for CPU 1 only:",
      "syntax": "sudo cpufreq-aperf -c {{1}}"
    },
    {
      "description": "Start calculating with a 3 second refresh interval for all CPU cores:",
      "syntax": "sudo cpufreq-aperf -i {{3}}"
    },
    {
      "description": "Calculate only once:",
      "syntax": "sudo cpufreq-aperf -o"
    }
  ]
}