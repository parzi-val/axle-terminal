{
  "command": "portablectl",
  "description": "A systemd utility for managing and deploying portable service images on Linux systems. More information: https://www.freedesktop.org/software/systemd/man/portablectl.html.",
  "useCases": [
    {
      "description": "List available portable service images discovered in the portable image search paths:",
      "syntax": "portablectl list"
    },
    {
      "description": "Attach a portable service image to the host system:",
      "syntax": "portablectl attach {{path/to/image}}"
    },
    {
      "description": "Detach a portable service image from the host system:",
      "syntax": "portablectl detach {{path/to/image|image_name}}"
    },
    {
      "description": "Display details and metadata about a specified portable service image:",
      "syntax": "portablectl inspect {{path/to/image}}"
    },
    {
      "description": "Check if a portable service image is attached to the host system:",
      "syntax": "portablectl is-attached {{path/to/image|image_name}}"
    }
  ]
}