{
  "command": "ark",
  "description": "KDE's archiving tool. More information: https://docs.kde.org/stable5/en/ark/ark/.",
  "useCases": [
    {
      "description": "Extract a specific archive into the current directory:",
      "syntax": "ark --batch {{path/to/archive}}"
    },
    {
      "description": "Extract an archive into a specific directory:",
      "syntax": "ark --batch --destination {{path/to/directory}} {{path/to/archive}}"
    },
    {
      "description": "Create an archive if it does not exist and add specific files to it:",
      "syntax": "ark --add-to {{path/to/archive}} {{path/to/file1 path/to/file2 ...}}"
    }
  ]
}