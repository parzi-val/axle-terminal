{
  "command": "getcap",
  "description": "Command to display the name and capabilities of each specified file. More information: https://manned.org/getcap.",
  "useCases": [
    {
      "description": "Get capabilities for the given files:",
      "syntax": "getcap {{path/to/file1 path/to/file2 ...}}"
    },
    {
      "description": "Get capabilities for all the files recursively under the given directories:",
      "syntax": "getcap -r {{path/to/directory1 path/to/directory2 ...}}"
    },
    {
      "description": "Displays all searched entries even if no capabilities are set:",
      "syntax": "getcap -v {{path/to/file1 path/to/file2 ...}}"
    }
  ]
}