{
  "command": "diffimg",
  "description": "Calculate intersection between two images. Note: the supported extensions are .png, .gif, .jpg, .ps. More information: https://manned.org/diffimg.",
  "useCases": [
    {
      "description": "Calculate the intersection between images and output an image where each pixel is the difference between corresponding pixels in input images:",
      "syntax": "diffimg {{path/to/input_image1.ext}} {{path/to/input_image2.ext}} {{path/to/output_image.ext}}"
    }
  ]
}