{
  "command": "unshadow",
  "description": "Utility provided by the John the Ripper project to obtain the traditional Unix password file if the system uses shadow passwords. More information: https://www.openwall.com/john/.",
  "useCases": [
    {
      "description": "Combine the /etc/shadow and /etc/passwd of the current system:",
      "syntax": "sudo unshadow /etc/passwd /etc/shadow"
    },
    {
      "description": "Combine two arbitrary shadow and password files:",
      "syntax": "sudo unshadow {{path/to/passwd}} {{path/to/shadow}}"
    }
  ]
}