{
  "command": "veracrypt",
  "description": "Free and open source disk encryption software. More information: https://www.veracrypt.fr/code/VeraCrypt/plain/doc/html/Documentation.html.",
  "useCases": [
    {
      "description": "Create a new volume through a text user interface and use /dev/urandom as a source of random data:",
      "syntax": "veracrypt --text --create --random-source={{/dev/urandom}}"
    },
    {
      "description": "Decrypt a volume interactively through a text user interface and mount it to a directory:",
      "syntax": "veracrypt --text {{path/to/volume}} {{path/to/mount_point}}"
    },
    {
      "description": "Decrypt a partition using a keyfile and mount it to a directory:",
      "syntax": "veracrypt --keyfiles={{path/to/keyfile}} {{/dev/sdXN}} {{path/to/mount_point}}"
    },
    {
      "description": "Dismount a volume on the directory it is mounted to:",
      "syntax": "veracrypt --dismount {{path/to/mounted_point}}"
    }
  ]
}