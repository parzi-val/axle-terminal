{
  "command": "iptables-restore",
  "description": "Restore the iptables IPv4 configuration. Use ip6tables-restore to do the same for IPv6. More information: https://manned.org/iptables-restore.",
  "useCases": [
    {
      "description": "Restore the iptables configuration from a file:",
      "syntax": "sudo iptables-restore {{path/to/file}}"
    }
  ]
}