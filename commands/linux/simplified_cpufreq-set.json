{
  "command": "cpufreq-set",
  "description": "A tool to modify CPU frequency settings. The frequency value should range between the output of command cpufreq-info -l. More information: https://manned.org/cpufreq-set.",
  "useCases": [
    {
      "description": "Set the CPU frequency policy of CPU 1 to \"userspace\":",
      "syntax": "sudo cpufreq-set -c {{1}} -g {{userspace}}"
    },
    {
      "description": "Set the current minimum CPU frequency of CPU 1:",
      "syntax": "sudo cpufreq-set -c {{1}} --min {{min_frequency}}"
    },
    {
      "description": "Set the current maximum CPU frequency of CPU 1:",
      "syntax": "sudo cpufreq-set -c {{1}} --max {{max_frequency}}"
    },
    {
      "description": "Set the current work frequency of CPU 1:",
      "syntax": "sudo cpufreq-set -c {{1}} -f {{work_frequency}}"
    }
  ]
}