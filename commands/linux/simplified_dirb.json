{
  "command": "dirb",
  "description": "Scan HTTP-based webservers for directories and files. More information: https://dirb.sourceforge.net.",
  "useCases": [
    {
      "description": "Scan a webserver using the default wordlist:",
      "syntax": "dirb {{https://example.org}}"
    },
    {
      "description": "Scan a webserver using a custom wordlist:",
      "syntax": "dirb {{https://example.org}} {{path/to/wordlist.txt}}"
    },
    {
      "description": "Scan a webserver non-recursively:",
      "syntax": "dirb {{https://example.org}} -r"
    },
    {
      "description": "Scan a webserver using a specified user-agent and cookie for HTTP-requests:",
      "syntax": "dirb {{https://example.org}} -a {{user_agent_string}} -c {{cookie_string}}"
    }
  ]
}