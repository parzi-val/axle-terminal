{
  "command": "abroot",
  "description": "Utility providing full immutability and atomicity by transacting between 2 root partition states (A⟺B). Updates are performed using OCI images, to ensure that the system is always in a consistent state. More information: https://github.com/Vanilla-OS/ABRoot.",
  "useCases": [
    {
      "description": "Add packages to the local image (Note: after executing this command, you need to apply these changes.):",
      "syntax": "sudo abroot pkg add {{package}}"
    },
    {
      "description": "Remove packages from the local image (Note: after executing this command, you need to apply these changes.):",
      "syntax": "sudo abroot pkg remove {{package}}"
    },
    {
      "description": "List packages in the local image:",
      "syntax": "sudo abroot pkg list"
    },
    {
      "description": "Apply changes in the local image (Note: you need to reboot your system for these changes to be applied):",
      "syntax": "sudo abroot pkg apply"
    },
    {
      "description": "Rollback your system to previous state:",
      "syntax": "sudo abroot rollback"
    },
    {
      "description": "Edit/View kernel parameters:",
      "syntax": "sudo abroot kargs {{edit|show}}"
    },
    {
      "description": "Display status:",
      "syntax": "sudo abroot status"
    },
    {
      "description": "Display help:",
      "syntax": "abroot --help"
    }
  ]
}