{
  "command": "microcom",
  "description": "A minimalistic terminal program, used to access remote devices via a serial, CAN or telnet connection from the console. More information: https://manned.org/microcom.",
  "useCases": [
    {
      "description": "Open a serial port using the specified baud rate:",
      "syntax": "microcom --port {{path/to/serial_port}} --speed {{baud_rate}}"
    },
    {
      "description": "Establish a telnet connection to the specified host:",
      "syntax": "microcom --telnet {{hostname}}:{{port}}"
    }
  ]
}