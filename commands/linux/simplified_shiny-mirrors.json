{
  "command": "shiny-mirrors",
  "description": "Generate a pacman mirror list for Manjaro Linux. Every run of shiny-mirrors requires you to synchronize your database and update your system using sudo pacman -Syyu. More information: https://gitlab.com/Arisa_Snowbell/shiny-mirrors/-/blob/domina/shiny-mirrors/man/shiny-mirrors.md.",
  "useCases": [
    {
      "description": "Get the status of the current mirrors:",
      "syntax": "shiny-mirrors status"
    },
    {
      "description": "Generate a mirror list using the default behavior:",
      "syntax": "sudo shiny-mirrors refresh"
    },
    {
      "description": "Display the current configuration file:",
      "syntax": "shiny-mirrors config show"
    },
    {
      "description": "Switch to a different branch interactively:",
      "syntax": "sudo shiny-mirrors config --branch"
    }
  ]
}