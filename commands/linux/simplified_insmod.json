{
  "command": "insmod",
  "description": "Dynamically load modules into the Linux Kernel. See also: kmod, for other module management commands. More information: https://manned.org/insmod.",
  "useCases": [
    {
      "description": "Insert a kernel module into the Linux kernel:",
      "syntax": "sudo insmod {{path/to/module.ko}}"
    }
  ]
}