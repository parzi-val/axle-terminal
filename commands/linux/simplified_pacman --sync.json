{
  "command": "pacman --sync",
  "description": "Arch Linux package manager utility. See also: pacman. More information: https://manned.org/pacman.8.",
  "useCases": [
    {
      "description": "Install a new package:",
      "syntax": "sudo pacman -S {{package}}"
    },
    {
      "description": "[S]ynchronize and refresh ([y]) the package database along with a sys[u]pgrade (add --downloadonly to only download the packages and not update them):",
      "syntax": "sudo pacman -Syu"
    },
    {
      "description": "Update and [u]pgrade all packages and install a new one without prompting:",
      "syntax": "sudo pacman -Syu --noconfirm {{package}}"
    },
    {
      "description": "[s]earch the package database for a regular expression or keyword:",
      "syntax": "pacman -Ss &quot;{{search_pattern}}&quot;"
    },
    {
      "description": "Display [i]nformation about a package:",
      "syntax": "pacman -Si {{package}}"
    },
    {
      "description": "Overwrite conflicting files during a package update:",
      "syntax": "sudo pacman -Syu --overwrite {{path/to/file}}"
    },
    {
      "description": "Remove not installed packages and unused repositories from the cache (use the flags Sc to [c]lean all packages):",
      "syntax": "sudo pacman -Sc"
    },
    {
      "description": "Specify the package version that should be installed:",
      "syntax": "sudo pacman -S {{package}}={{version}}"
    }
  ]
}