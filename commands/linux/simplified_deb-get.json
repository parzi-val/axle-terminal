{
  "command": "deb-get",
  "description": "apt-get functionality for .deb packages published in third party repositories or via direct download. Works with Linux distributions which use apt-get. More information: https://github.com/wimpysworld/deb-get.",
  "useCases": [
    {
      "description": "Update the list of available packages and versions:",
      "syntax": "deb-get update"
    },
    {
      "description": "Search for a given package:",
      "syntax": "deb-get search {{package}}"
    },
    {
      "description": "Show information about a package:",
      "syntax": "deb-get show {{package}}"
    },
    {
      "description": "Install a package, or update it to the latest available version:",
      "syntax": "deb-get install {{package}}"
    },
    {
      "description": "Remove a package (using purge instead also removes its configuration files):",
      "syntax": "deb-get remove {{package}}"
    },
    {
      "description": "Upgrade all installed packages to their newest available versions:",
      "syntax": "deb-get upgrade"
    },
    {
      "description": "List all available packages:",
      "syntax": "deb-get list"
    }
  ]
}