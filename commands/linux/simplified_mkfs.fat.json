{
  "command": "mkfs.fat",
  "description": "Create an MS-DOS filesystem inside a partition. More information: https://manned.org/mkfs.fat.",
  "useCases": [
    {
      "description": "Create a fat filesystem inside partition 1 on device b (sdb1):",
      "syntax": "mkfs.fat {{/dev/sdb1}}"
    },
    {
      "description": "Create filesystem with a volume-name:",
      "syntax": "mkfs.fat -n {{volume_name}} {{/dev/sdb1}}"
    },
    {
      "description": "Create filesystem with a volume-id:",
      "syntax": "mkfs.fat -i {{volume_id}} {{/dev/sdb1}}"
    },
    {
      "description": "Use 5 instead of 2 file allocation tables:",
      "syntax": "mkfs.fat -f 5 {{/dev/sdb1}}"
    }
  ]
}