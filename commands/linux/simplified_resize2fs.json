{
  "command": "resize2fs",
  "description": "Resize an ext2, ext3 or ext4 filesystem. Does not resize the underlying partition. The filesystem may have to be unmounted first, read the man page for more details. More information: https://manned.org/resize2fs.",
  "useCases": [
    {
      "description": "Automatically resize a filesystem:",
      "syntax": "resize2fs {{/dev/sdXN}}"
    },
    {
      "description": "Resize the filesystem to a size of 40G, displaying a progress bar:",
      "syntax": "resize2fs -p {{/dev/sdXN}} {{40G}}"
    },
    {
      "description": "Shrink the filesystem to its minimum possible size:",
      "syntax": "resize2fs -M {{/dev/sdXN}}"
    }
  ]
}