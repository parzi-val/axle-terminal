{
  "command": "mate-search-tool",
  "description": "Search files in MATE desktop environment. More information: https://manned.org/mate-search-tool.",
  "useCases": [
    {
      "description": "Search files containing a specific string in their name in a specific directory:",
      "syntax": "mate-search-tool --named={{string}} --path={{path/to/directory}}"
    },
    {
      "description": "Search files without waiting a user confirmation:",
      "syntax": "mate-search-tool --start --named={{string}} --path={{path/to/directory}}"
    },
    {
      "description": "Search files with name matching a specific regular expression:",
      "syntax": "mate-search-tool --start --regex={{string}} --path={{path/to/directory}}"
    },
    {
      "description": "Set a sorting order in search results:",
      "syntax": "mate-search-tool --start --named={{string}} --path={{path/to/directory}} --sortby={{name|folder|size|type|date}}"
    },
    {
      "description": "Set a descending sorting order:",
      "syntax": "mate-search-tool --start --named={{string}} --path={{path/to/directory}} --descending"
    },
    {
      "description": "Search files owned by a specific user/group:",
      "syntax": "mate-search-tool --start --{{user|group}}={{value}} --path={{path/to/directory}}"
    }
  ]
}