{
  "command": "filefrag",
  "description": "Report how badly fragmented a particular file might be. More information: https://manned.org/filefrag.",
  "useCases": [
    {
      "description": "Display a report for one or more files:",
      "syntax": "filefrag {{path/to/file1 path/to/file2 ...}}"
    },
    {
      "description": "Display a report using a 1024 byte blocksize:",
      "syntax": "filefrag -k {{path/to/file}}"
    },
    {
      "description": "Display a report using a certain blocksize:",
      "syntax": "filefrag -b{{1024|1K|1M|1G|...}} {{path/to/file}}"
    },
    {
      "description": "Sync the file before requesting the mapping:",
      "syntax": "filefrag -s {{path/to/file1 path/to/file2 ...}}"
    },
    {
      "description": "Display mapping of extended attributes:",
      "syntax": "filefrag -x {{path/to/file1 path/to/file2 ...}}"
    },
    {
      "description": "Display a report with verbose information:",
      "syntax": "filefrag -v {{path/to/file1 path/to/file2 ...}}"
    }
  ]
}