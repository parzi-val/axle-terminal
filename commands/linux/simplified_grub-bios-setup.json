{
  "command": "grub-bios-setup",
  "description": "Set up a device to use GRUB with a BIOS configuration. You should use grub-install instead of grub-bios-setup in most cases. More information: https://manned.org/grub-bios-setup.8.",
  "useCases": [
    {
      "description": "Set up a device to boot with GRUB:",
      "syntax": "grub-bios-setup {{/dev/sdX}}"
    },
    {
      "description": "Install even if problems are detected:",
      "syntax": "grub-bios-setup --force {{/dev/sdX}}"
    },
    {
      "description": "Install GRUB in a specific directory:",
      "syntax": "grub-bios-setup --directory={{/boot/grub}} {{/dev/sdX}}"
    }
  ]
}