{
  "command": "grub-set-default",
  "description": "Set the default boot entry for GRUB. More information: https://manned.org/grub-set-default.",
  "useCases": [
    {
      "description": "Set the default boot entry to an entry number, name or identifier:",
      "syntax": "sudo grub-set-default {{entry_number}}"
    },
    {
      "description": "Set the default boot entry to an entry number, name or identifier for an alternative boot directory:",
      "syntax": "sudo grub-set-default --boot-directory {{/path/to/boot_directory}} {{entry_number}}"
    }
  ]
}