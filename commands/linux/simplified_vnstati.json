{
  "command": "vnstati",
  "description": "PNG image output support for vnStat. More information: https://manned.org/vnstati.",
  "useCases": [
    {
      "description": "Output a summary of the last 2: months, days, and all-time:",
      "syntax": "vnstati --summary --iface {{network_interface}} --output {{path/to/output.png}}"
    },
    {
      "description": "Output the 10 most traffic-intensive days of all time:",
      "syntax": "vnstati --top 10 --iface {{network_interface}} --output {{path/to/output.png}}"
    },
    {
      "description": "Output monthly traffic statistics from the last 12 months:",
      "syntax": "vnstati --months --iface {{network_interface}} --output {{path/to/output.png}}"
    },
    {
      "description": "Output hourly traffic statistics from the last 24 hours:",
      "syntax": "vnstati --hours --iface {{network_interface}} --output {{path/to/output.png}}"
    }
  ]
}