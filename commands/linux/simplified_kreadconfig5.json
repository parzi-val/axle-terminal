{
  "command": "kreadconfig5",
  "description": "Read KConfig entries for KDE Plasma. More information: https://userbase.kde.org/KDE_System_Administration/Configuration_Files.",
  "useCases": [
    {
      "description": "Read a key from the global configuration:",
      "syntax": "kreadconfig5 --group {{group_name}} --key {{key_name}}"
    },
    {
      "description": "Read a key from a specific configuration file:",
      "syntax": "kwriteconfig5 --file {{path/to/file}} --group {{group_name}} --key {{key_name}}"
    },
    {
      "description": "Check if systemd is used to start the Plasma session:",
      "syntax": "kreadconfig5 --file {{startkderc}} --group {{General}} --key {{systemdBoot}}"
    }
  ]
}