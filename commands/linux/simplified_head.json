{
  "command": "head",
  "description": "Output the first part of files. More information: https://www.gnu.org/software/coreutils/manual/html_node/head-invocation.html.",
  "useCases": [
    {
      "description": "Output the first few lines of a file:",
      "syntax": "head --lines {{count}} {{path/to/file}}"
    },
    {
      "description": "Output the first few bytes of a file:",
      "syntax": "head --bytes {{count}} {{path/to/file}}"
    },
    {
      "description": "Output everything but the last few lines of a file:",
      "syntax": "head --lines -{{count}} {{path/to/file}}"
    },
    {
      "description": "Output everything but the last few bytes of a file:",
      "syntax": "head --bytes -{{count}} {{path/to/file}}"
    }
  ]
}