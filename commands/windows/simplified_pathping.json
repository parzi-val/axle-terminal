{
  "command": "pathping",
  "description": "A trace route tool combining features of ping and tracert. More information: https://learn.microsoft.com/windows-server/administration/windows-commands/pathping.",
  "useCases": [
    {
      "description": "Ping and trace the route to a host:",
      "syntax": "pathping {{hostname}}"
    },
    {
      "description": "Do not perform reverse lookup of IP address to hostname:",
      "syntax": "pathping {{hostname}} -n"
    },
    {
      "description": "Specify the maximum number of hops to search for the target (the default is 30):",
      "syntax": "pathping {{hostname}} -h {{max_hops}}"
    },
    {
      "description": "Specify the milliseconds to wait between pings (the default is 240):",
      "syntax": "pathping {{hostname}} -p {{time}}"
    },
    {
      "description": "Specify the number of queries per hop (the default is 100):",
      "syntax": "pathping {{hostname}} -q {{queries}}"
    },
    {
      "description": "Force IPV4 usage:",
      "syntax": "pathping {{hostname}} -4"
    },
    {
      "description": "Force IPV6 usage:",
      "syntax": "pathping {{hostname}} -6"
    },
    {
      "description": "Display help:",
      "syntax": "pathping /?"
    }
  ]
}