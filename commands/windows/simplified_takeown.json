{
  "command": "takeown",
  "description": "Take ownership of a file or directory. More information: https://learn.microsoft.com/windows-server/administration/windows-commands/takeown.",
  "useCases": [
    {
      "description": "Take ownership of the specified file:",
      "syntax": "takeown /f {{path\\to\\file}}"
    },
    {
      "description": "Take ownership of the specified directory:",
      "syntax": "takeown /d {{path\\to\\directory}}"
    },
    {
      "description": "Take ownership of the specified directory and all subdirectories:",
      "syntax": "takeown /r /d {{path\\to\\directory}}"
    },
    {
      "description": "Change ownership to the Administrator group instead of the current user:",
      "syntax": "takeown /a /f {{path\\to\\file}}"
    }
  ]
}