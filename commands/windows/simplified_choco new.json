{
  "command": "choco new",
  "description": "Generate new package specification files with Chocolatey. More information: https://chocolatey.org/docs/commands-new.",
  "useCases": [
    {
      "description": "Create a new package skeleton:",
      "syntax": "choco new {{package}}"
    },
    {
      "description": "Create a new package with a specific version:",
      "syntax": "choco new {{package}} --version {{version}}"
    },
    {
      "description": "Create a new package with a specific maintainer name:",
      "syntax": "choco new {{package}} --maintainer {{maintainer_name}}"
    },
    {
      "description": "Create a new package in a custom output directory:",
      "syntax": "choco new {{package}} --output-directory {{path/to/directory}}"
    },
    {
      "description": "Create a new package with specific 32-bit and 64-bit installer URLs:",
      "syntax": "choco new {{package}} url=&quot;{{url}}&quot; url64=&quot;{{url}}&quot;"
    }
  ]
}