{
  "command": "reg unload",
  "description": "Remove data from the registry that was loaded using the reg load command. More information: https://learn.microsoft.com/windows-server/administration/windows-commands/reg-unload.",
  "useCases": [
    {
      "description": "Remove data from the registry for a specified key:",
      "syntax": "reg unload {{key_name}}"
    }
  ]
}