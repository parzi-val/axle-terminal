{
  "command": "Invoke-Item",
  "description": "Open files in their respective default programs. Note: This command can only be used through PowerShell. More information: https://learn.microsoft.com/powershell/module/microsoft.powershell.management/invoke-item.",
  "useCases": [
    {
      "description": "Open a file in its default program:",
      "syntax": "Invoke-Item -Path {{path\\to\\file}}"
    },
    {
      "description": "Open all files inside a directory:",
      "syntax": "Invoke-Item -Path {{path\\to\\directory}}\\*"
    },
    {
      "description": "Open all PNGs inside a directory:",
      "syntax": "Invoke-Item -Path {{path\\to\\directory}}\\*.png"
    },
    {
      "description": "Open all files inside a directory containing a specific keyword:",
      "syntax": "Invoke-Item -Path {{path\\to\\directory}}\\* -Include {{*keyword*}}"
    },
    {
      "description": "Open all files inside a directory except those containing a specific keyword:",
      "syntax": "Invoke-Item -Path {{path\\to\\directory}}\\* -Exclude {{*keyword*}}"
    },
    {
      "description": "Perform a dry run to determine which files will be opened inside a directory through Invoke-Item:",
      "syntax": "Invoke-Item -Path {{path\\to\\directory}}\\* -WhatIf"
    }
  ]
}