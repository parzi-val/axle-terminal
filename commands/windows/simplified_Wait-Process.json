{
  "command": "Wait-Process",
  "description": "Waits for the processes to be stopped before accepting more input. Note: This command can only be used through PowerShell. More information: https://learn.microsoft.com/powershell/module/microsoft.powershell.management/wait-process.",
  "useCases": [
    {
      "description": "Stop a process and wait:",
      "syntax": "Stop-Process -Id {{process_id}}; Wait-Process -Id {{process_id}}"
    },
    {
      "description": "Wait for processes for a specified time:",
      "syntax": "Wait-Process -Name {{process_name}} -Timeout {{30}}"
    }
  ]
}