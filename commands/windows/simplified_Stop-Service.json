{
  "command": "Stop-Service",
  "description": "Stops running services. Note: This command can only be used through PowerShell. More information: https://learn.microsoft.com/powershell/module/microsoft.powershell.management/stop-service.",
  "useCases": [
    {
      "description": "Stop a service on the local computer:",
      "syntax": "Stop-Service -Name {{service_name}}"
    },
    {
      "description": "Stop a service by using the display name:",
      "syntax": "Stop-Service -DisplayName &quot;{{name}}&quot;"
    },
    {
      "description": "Stop a service that has dependent services:",
      "syntax": "Stop-Service -Name {{service_name}} -Force -Confirm"
    }
  ]
}