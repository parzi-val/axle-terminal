{
  "command": "choco-push",
  "description": "Push a compiled NuGet package (nupkg) to a package feed. More information: https://docs.chocolatey.org/en-us/create/commands/push.",
  "useCases": [
    {
      "description": "Push a compiled nupkg to the specified feed:",
      "syntax": "choco push --source {{https://push.chocolatey.org/}}"
    },
    {
      "description": "Push a compiled nupkg to the specified feed with a timeout in seconds (default is 2700):",
      "syntax": "choco push --source {{https://push.chocolatey.org/}} --execution-timeout {{500}}"
    }
  ]
}