{
  "command": "Set-Service",
  "description": "Starts, stops, and suspends a service, and changes its properties. Note: This command can only be used through PowerShell. More information: https://learn.microsoft.com/powershell/module/microsoft.powershell.management/set-service.",
  "useCases": [
    {
      "description": "Change a display name:",
      "syntax": "Set-Service -Name {{hostname}} -DisplayName &quot;{{name}}&quot;"
    },
    {
      "description": "Change the startup type of services:",
      "syntax": "Set-Service -Name {{service_name}} -StartupType {{Automatic}}"
    },
    {
      "description": "Change the description of a service:",
      "syntax": "Set-Service -Name {{service_name}} -Description &quot;{{description}}&quot;"
    }
  ]
}