{
  "command": "scoop bucket",
  "description": "Manage buckets: Git repositories containing files which describe how scoop installs applications. If Scoop doesn't know where the bucket is located its repository location must be specified. More information: https://github.com/lukesampson/scoop/wiki/Buckets.",
  "useCases": [
    {
      "description": "List all buckets currently in use:",
      "syntax": "scoop bucket list"
    },
    {
      "description": "List all known buckets:",
      "syntax": "scoop bucket known"
    },
    {
      "description": "Add a known bucket by its name:",
      "syntax": "scoop bucket add {{name}}"
    },
    {
      "description": "Add an unknown bucket by its name and Git repository URL:",
      "syntax": "scoop bucket add {{name}} {{https://example.com/repository.git}}"
    },
    {
      "description": "Remove a bucket by its name:",
      "syntax": "scoop bucket rm {{name}}"
    }
  ]
}