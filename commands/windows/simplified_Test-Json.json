{
  "command": "Test-Json",
  "description": "Test whether a string is a valid JSON document. Note: This command can only be used through PowerShell. More information: https://learn.microsoft.com/powershell/module/microsoft.powershell.utility/test-json.",
  "useCases": [
    {
      "description": "Test if a string from stdin is in JSON format:",
      "syntax": "'{{string}}' | Test-Json"
    },
    {
      "description": "Test if a string JSON format:",
      "syntax": "Test-Json -Json '{{json_to_test}}'"
    },
    {
      "description": "Test if a string from stdin matches a specific schema file:",
      "syntax": "'{{string}}' | Test-Json -SchemaFile {{path\\to\\schema_file.json}}"
    }
  ]
}