{
  "command": "Out-String",
  "description": "Outputs input objects as a string. Note: This command can only be used through PowerShell. More information: https://learn.microsoft.com/powershell/module/microsoft.powershell.utility/out-string.",
  "useCases": [
    {
      "description": "Print host information as string:",
      "syntax": "Get-Alias | Out-String"
    },
    {
      "description": "Convert each object to a string rather than concatenating all the objects into a single string:",
      "syntax": "Get-Alias | Out-String -Stream"
    },
    {
      "description": "Use the Width parameter to prevent truncation:",
      "syntax": "@{TestKey = ('x' * 200)} | Out-String -Width {{250}}"
    }
  ]
}