{
  "command": "Get-ChildItem",
  "description": "List items in a directory. Note: This command can only be used through PowerShell. More information: https://learn.microsoft.com/powershell/module/microsoft.powershell.management/get-childitem.",
  "useCases": [
    {
      "description": "List all non-hidden items in the current directory:",
      "syntax": "Get-ChildItem"
    },
    {
      "description": "List only directories in the current directory:",
      "syntax": "Get-ChildItem -Directory"
    },
    {
      "description": "List only files in the current directory:",
      "syntax": "Get-ChildItem -File"
    },
    {
      "description": "List items in the current directory, including hidden items:",
      "syntax": "Get-ChildItem -Hidden"
    },
    {
      "description": "List items in a directory other than the current one:",
      "syntax": "Get-ChildItem -Path {{path\\to\\directory}}"
    }
  ]
}