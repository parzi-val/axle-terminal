{
  "command": "choco pin",
  "description": "Pin a package at a version with Chocolatey. Pinned packages are skipped automatically when upgrading. More information: https://chocolatey.org/docs/commands-pin.",
  "useCases": [
    {
      "description": "Display a list of pinned packages and their versions:",
      "syntax": "choco pin list"
    },
    {
      "description": "Pin a package at its current version:",
      "syntax": "choco pin add --name {{package}}"
    },
    {
      "description": "Pin a package at a specific version:",
      "syntax": "choco pin add --name {{package}} --version {{version}}"
    },
    {
      "description": "Remove a pin for a specific package:",
      "syntax": "choco pin remove --name {{package}}"
    }
  ]
}