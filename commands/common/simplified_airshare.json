{
  "command": "airshare",
  "description": "Transfer data between two machines in a local network. More information: https://airshare.rtfd.io/en/latest/cli.html.",
  "useCases": [
    {
      "description": "Share files or directories:",
      "syntax": "airshare {{code}} {{path/to/file_or_directory1 path/to/file_or_directory2 ...}}"
    },
    {
      "description": "Receive a file:",
      "syntax": "airshare {{code}}"
    },
    {
      "description": "Host a receiving server (use this to be able to upload files using the web interface):",
      "syntax": "airshare --upload {{code}}"
    },
    {
      "description": "Send files or directories to a receiving server:",
      "syntax": "airshare --upload {{code}} {{path/to/file_or_directory1 path/to/file_or_directory2 ...}}"
    },
    {
      "description": "Send files whose paths have been copied to the clipboard:",
      "syntax": "airshare --file-path {{code}}"
    },
    {
      "description": "Receive a file and copy it to the clipboard:",
      "syntax": "airshare --clip-receive {{code}}"
    }
  ]
}