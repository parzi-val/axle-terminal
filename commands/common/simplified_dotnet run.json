{
  "command": "dotnet run",
  "description": "Run a .NET application without explicit compile or launch commands. More information: https://learn.microsoft.com/dotnet/core/tools/dotnet-run.",
  "useCases": [
    {
      "description": "Run the project in the current directory:",
      "syntax": "dotnet run"
    },
    {
      "description": "Run a specific project:",
      "syntax": "dotnet run --project {{path/to/file.csproj}}"
    },
    {
      "description": "Run the project with specific arguments:",
      "syntax": "dotnet run -- {{arg1=foo arg2=bar ...}}"
    },
    {
      "description": "Run the project using a target framework moniker:",
      "syntax": "dotnet run --framework {{net7.0}}"
    },
    {
      "description": "Specify architecture and OS, available since .NET 6 (Don't use --runtime with these options):",
      "syntax": "dotnet run --arch {{x86|x64|arm|arm64}} --os {{win|win7|osx|linux|ios|android}}"
    }
  ]
}