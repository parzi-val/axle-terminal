{
  "command": "webstorm",
  "description": "The JetBrains JavaScript IDE. More information: https://www.jetbrains.com/help/webstorm/working-with-the-ide-features-from-command-line.html.",
  "useCases": [
    {
      "description": "Open the current directory in WebStorm:",
      "syntax": "webstorm"
    },
    {
      "description": "Open a specific directory in WebStorm:",
      "syntax": "webstorm {{path/to/directory}}"
    },
    {
      "description": "Open specific files in the LightEdit mode﻿:",
      "syntax": "webstorm -e {{path/to/file1 path/to/file2 ...}}"
    },
    {
      "description": "Open and wait until done editing a specific file in the LightEdit mode:",
      "syntax": "webstorm --wait -e {{path/to/file}}"
    },
    {
      "description": "Open a file with the cursor at the specific line:",
      "syntax": "webstorm --line {{line_number}} {{path/to/file}}"
    },
    {
      "description": "Open and compare files (supports up to 3 files):",
      "syntax": "webstorm diff {{path/to/file1 path/to/file2 path/to/optional_file3}}"
    },
    {
      "description": "Open and perform a three-way merge:",
      "syntax": "webstorm merge {{path/to/left_file}} {{path/to/right_file}} {{path/to/target_file}}"
    }
  ]
}