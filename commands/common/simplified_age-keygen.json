{
  "command": "age-keygen",
  "description": "Generate age key pairs. See also: age for encrypting/decrypting files. More information: https://manned.org/age-keygen.",
  "useCases": [
    {
      "description": "Generate a key pair, save it to an unencrypted file, and print the public key to stdout:",
      "syntax": "age-keygen --output {{path/to/file}}"
    },
    {
      "description": "Convert an identit[y] to a recipient and print the public key to stdout:",
      "syntax": "age-keygen -y {{path/to/file}}"
    }
  ]
}