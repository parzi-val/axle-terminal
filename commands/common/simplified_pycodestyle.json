{
  "command": "pycodestyle",
  "description": "Check Python code against PEP 8 style conventions. More information: https://pycodestyle.readthedocs.io.",
  "useCases": [
    {
      "description": "Check the style of a single file:",
      "syntax": "pycodestyle {{file.py}}"
    },
    {
      "description": "Check the style of multiple files:",
      "syntax": "pycodestyle {{file1.py file2.py ...}}"
    },
    {
      "description": "Show only the first occurrence of an error:",
      "syntax": "pycodestyle --first {{file.py}}"
    },
    {
      "description": "Show the source code for each error:",
      "syntax": "pycodestyle --show-source {{file.py}}"
    },
    {
      "description": "Show the specific PEP 8 text for each error:",
      "syntax": "pycodestyle --show-pep8 {{file.py}}"
    }
  ]
}