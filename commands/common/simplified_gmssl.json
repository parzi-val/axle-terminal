{
  "command": "gmssl",
  "description": "GmSSL is a crypto toolkit supporting SM1, SM2, SM3, SM4, SM9, and ZUC/ZUC256. More information: http://gmssl.org/english.html.",
  "useCases": [
    {
      "description": "Generate an SM3 hash for a file:",
      "syntax": "gmssl sm3 {{path/to/file}}"
    },
    {
      "description": "Encrypt a file using the SM4 cipher:",
      "syntax": "gmssl sms4 -e -in {{path/to/file}} -out {{path/to/file.sms4}}"
    },
    {
      "description": "Decrypt a file using the SM4 cipher:",
      "syntax": "gmssl sms4 -d -in {{path/to/file.sms4}}"
    },
    {
      "description": "Generate an SM2 private key:",
      "syntax": "gmssl sm2 -genkey -out {{path/to/file.pem}}"
    },
    {
      "description": "Generate an SM2 public key from an existing private key:",
      "syntax": "gmssl sm2 -pubout -in {{path/to/file.pem}} -out {{path/to/file.pem.pub}}"
    },
    {
      "description": "Encrypt a file using the ZUC cipher:",
      "syntax": "gmssl zuc -e -in {{path/to/file}} -out {{path/to/file.zuc}}"
    },
    {
      "description": "Decrypt a file using the ZUC cipher:",
      "syntax": "gmssl zuc -d -in {{path/to/file.zuc}}"
    },
    {
      "description": "Display version:",
      "syntax": "gmssl version"
    }
  ]
}