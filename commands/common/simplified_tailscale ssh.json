{
  "command": "tailscale ssh",
  "description": "SSH to a Tailscale machine (Linux Only). More information: https://tailscale.com/kb/1193/tailscale-ssh.",
  "useCases": [
    {
      "description": "Advertise/Disable SSH on the host:",
      "syntax": "sudo tailscale up --ssh={{true|false}}"
    },
    {
      "description": "SSH to a specific host which has Tailscale-SSH enabled:",
      "syntax": "tailscale ssh {{username}}@{{host}}"
    }
  ]
}