{
  "command": "ifne",
  "description": "Run a command depending on the emptyness of stdin. More information: https://joeyh.name/code/moreutils/.",
  "useCases": [
    {
      "description": "Run the specified command if and only if stdin is not empty:",
      "syntax": "ifne {{command options ...}}"
    },
    {
      "description": "Run the specified command if and only if stdin is empty, otherwise pass stdin to stdout:",
      "syntax": "ifne -n {{command options ...}}"
    }
  ]
}