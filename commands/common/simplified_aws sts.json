{
  "command": "aws sts",
  "description": "Security Token Service (STS) allows to request temporary credentials for (IAM) users or federated users. More information: https://docs.aws.amazon.com/cli/latest/reference/sts/.",
  "useCases": [
    {
      "description": "Get temporary security credentials to access specific AWS resources:",
      "syntax": "aws sts assume-role --role-arn {{aws_role_arn}}"
    },
    {
      "description": "Get an IAM user or role whose credentials are used to call the operation:",
      "syntax": "aws sts get-caller-identity"
    }
  ]
}