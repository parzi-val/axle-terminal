{
  "command": "exec",
  "description": "Execute a command without creating a child process. More information: https://manned.org/exec.1posix.",
  "useCases": [
    {
      "description": "Execute a specific command using the current environment variables:",
      "syntax": "exec {{command -with -flags}}"
    }
  ]
}