{
  "command": "openconnect",
  "description": "A VPN client, for Cisco AnyConnect VPNs and others. More information: https://www.infradead.org/openconnect/manual.html.",
  "useCases": [
    {
      "description": "Connect to a server:",
      "syntax": "openconnect {{vpn.example.org}}"
    },
    {
      "description": "Connect to a server, forking into the background:",
      "syntax": "openconnect --background {{vpn.example.org}}"
    },
    {
      "description": "Terminate the connection that is running in the background:",
      "syntax": "killall -SIGINT openconnect"
    },
    {
      "description": "Connect to a server, reading options from a configuration file:",
      "syntax": "openconnect --config={{path/to/file}} {{vpn.example.org}}"
    },
    {
      "description": "Connect to a server and authenticate with a specific SSL client certificate:",
      "syntax": "openconnect --certificate={{path/to/file}} {{vpn.example.org}}"
    }
  ]
}