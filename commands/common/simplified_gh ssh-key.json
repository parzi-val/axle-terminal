{
  "command": "gh ssh-key",
  "description": "Manage GitHub SSH keys. More information: https://cli.github.com/manual/gh_ssh-key.",
  "useCases": [
    {
      "description": "Display help:",
      "syntax": "gh ssh-key"
    },
    {
      "description": "List SSH keys for the currently authenticated user:",
      "syntax": "gh ssh-key list"
    },
    {
      "description": "Add an SSH key to the currently authenticated user's account:",
      "syntax": "gh ssh-key add {{path/to/key.pub}}"
    },
    {
      "description": "Add an SSH key to the currently authenticated user's account with a specific title:",
      "syntax": "gh ssh-key add --title {{title}} {{path/to/key.pub}}"
    }
  ]
}