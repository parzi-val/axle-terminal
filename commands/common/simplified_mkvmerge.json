{
  "command": "mkvmerge",
  "description": "Merge and extract multimedia streams. More information: https://mkvtoolnix.download/doc/mkvmerge.html.",
  "useCases": [
    {
      "description": "Display information about a Matroska file:",
      "syntax": "mkvmerge --identify {{path/to/file.mkv}}"
    },
    {
      "description": "Extract the audio from track 1 of a specific file:",
      "syntax": "mkvextract tracks {{path/to/file.mkv}} {{1}}:{{path/to/output.webm}}"
    },
    {
      "description": "Extract the subtitle from track 3 of a specific file:",
      "syntax": "mkvextract tracks {{path/to/file.mkv}} {{3}}:{{path/to/subs.srt}}"
    },
    {
      "description": "Add a subtitle track to a file:",
      "syntax": "mkvmerge --output {{path/to/output.mkv}} {{path/to/file.mkv}} {{path/to/subs.srt}}"
    }
  ]
}