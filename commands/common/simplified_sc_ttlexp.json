{
  "command": "sc_ttlexp",
  "description": "Dump source addresses from ICMP TTL expired messages in warts files. More information: https://www.caida.org/catalog/software/scamper/.",
  "useCases": [
    {
      "description": "Output the source address of ICMP TTL expire messages in warts files one after the other:",
      "syntax": "sc_ttlexp {{path/to/file1.warts path/to/file2.warts ...}}"
    }
  ]
}