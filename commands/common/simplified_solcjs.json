{
  "command": "solcjs",
  "description": "A set of JavaScript bindings for the Solidity compiler. More information: https://github.com/ethereum/solc-js.",
  "useCases": [
    {
      "description": "Compile a specific contract to hex:",
      "syntax": "solcjs --bin {{path/to/file.sol}}"
    },
    {
      "description": "Compile the ABI of a specific contract:",
      "syntax": "solcjs --abi {{path/to/file.sol}}"
    },
    {
      "description": "Specify a base path to resolve imports from:",
      "syntax": "solcjs --bin --base-path {{path/to/directory}} {{path/to/file.sol}}"
    },
    {
      "description": "Specify one or more paths to include containing external code:",
      "syntax": "solcjs --bin --include-path {{path/to/directory}} {{path/to/file.sol}}"
    },
    {
      "description": "Optimise the generated bytecode:",
      "syntax": "solcjs --bin --optimize {{path/to/file.sol}}"
    }
  ]
}