{
  "command": "czkawka-cli",
  "description": "Command-line version of czkawka a multi-functional app to find duplicates, empty folders, similar images and much more. More information: https://github.com/qarmin/czkawka.",
  "useCases": [
    {
      "description": "List duplicate or similar files in specific directories:",
      "syntax": "czkawka-cli {{dup|image}} --directories {{path/to/directory1 path/to/directory2 ...}}"
    },
    {
      "description": "Find duplicate files in specific directories and delete them (default: NONE):",
      "syntax": "czkawka-cli dup --directories {{path/to/directory1 path/to/directory2 ...}} --delete-method {{AEN|AEO|ON|OO|HARD|NONE}}"
    }
  ]
}