{
  "command": "secrethub",
  "description": "Keep secrets out of config files. More information: https://github.com/secrethub/secrethub-cli.",
  "useCases": [
    {
      "description": "Print a secret to stdout:",
      "syntax": "secrethub read {{path/to/secret}}"
    },
    {
      "description": "Generate a random value and store it as a new or updated secret:",
      "syntax": "secrethub generate {{path/to/secret}}"
    },
    {
      "description": "Store a value from the clipboard as a new or updated secret:",
      "syntax": "secrethub write --clip {{path/to/secret}}"
    },
    {
      "description": "Store a value supplied on stdin as a new or updated secret:",
      "syntax": "echo &quot;{{secret_value}}&quot; | secrethub write {{path/to/secret}}"
    },
    {
      "description": "Audit a repository or secret:",
      "syntax": "secrethub audit {{path/to/repo_or_secret}}"
    }
  ]
}