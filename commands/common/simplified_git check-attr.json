{
  "command": "git check-attr",
  "description": "For every pathname, list if each attribute is unspecified, set, or unset as a gitattribute on that pathname. More information: https://git-scm.com/docs/git-check-attr.",
  "useCases": [
    {
      "description": "Check the values of all attributes on a file:",
      "syntax": "git check-attr --all {{path/to/file}}"
    },
    {
      "description": "Check the value of a specific attribute on a file:",
      "syntax": "git check-attr {{attribute}} {{path/to/file}}"
    },
    {
      "description": "Check the values of all attributes on specific files:",
      "syntax": "git check-attr --all {{path/to/file1 path/to/file2 ...}}"
    },
    {
      "description": "Check the value of a specific attribute on one or more files:",
      "syntax": "git check-attr {{attribute}} {{path/to/file1 path/to/file2 ...}}"
    }
  ]
}