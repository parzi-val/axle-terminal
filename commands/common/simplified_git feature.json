{
  "command": "git feature",
  "description": "Create or merge feature branches. Feature branches obey the format feature/. More information: https://github.com/tj/git-extras/blob/master/Commands.md#git-feature.",
  "useCases": [
    {
      "description": "Create and switch to a new feature branch:",
      "syntax": "git feature {{feature_branch}}"
    },
    {
      "description": "Merge a feature branch into the current branch creating a merge commit:",
      "syntax": "git feature finish {{feature_branch}}"
    },
    {
      "description": "Merge a feature branch into the current branch squashing the changes into one commit:",
      "syntax": "git feature finish --squash {{feature_branch}}"
    },
    {
      "description": "Send changes from a specific feature branch to its remote counterpart:",
      "syntax": "git feature {{feature_branch}} --remote {{remote_name}}"
    }
  ]
}