{
  "command": "gdal2tiles.py",
  "description": "Generate TMS or XYZ tiles for a raster dataset. More information: https://gdal.org/programs/gdal2tiles.html.",
  "useCases": [
    {
      "description": "Generate TMS tiles for the zoom levels 2 to 5 of a raster dataset:",
      "syntax": "gdal2tiles.py --zoom 2-5 {{path/to/input.tif}} {{path/to/output_directory}}"
    },
    {
      "description": "Generate XYZ tiles for the zoom levels 2 to 5 of a raster dataset:",
      "syntax": "gdal2tiles.py --zoom 2-5 --xyz {{path/to/input.tif}} {{path/to/output_directory}}"
    }
  ]
}