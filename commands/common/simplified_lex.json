{
  "command": "lex",
  "description": "Lexical analyzer generator. Given the specification for a lexical analyzer, generates C code implementing it. Note: on most major OSes, this command is an alias for flex. More information: https://manned.org/lex.1.",
  "useCases": [
    {
      "description": "Generate an analyzer from a Lex file, storing it to the file lex.yy.c:",
      "syntax": "lex {{analyzer.l}}"
    },
    {
      "description": "Specify the output file:",
      "syntax": "lex -t {{analyzer.l}} &gt; {{analyzer.c}}"
    },
    {
      "description": "Compile a C file generated by Lex:",
      "syntax": "c99 {{path/to/lex.yy.c}} -o {{executable}}"
    }
  ]
}