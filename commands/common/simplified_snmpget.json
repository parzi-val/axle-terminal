{
  "command": "snmpget",
  "description": "Query using the SNMP protocol. More information: https://manned.org/snmpget.",
  "useCases": [
    {
      "description": "Request a single value from the SNMP agent:",
      "syntax": "snmpget -v {{version}} -c {{community}} {{ip}} {{oid}}"
    },
    {
      "description": "Display the full Object Identifier (OID) path:",
      "syntax": "snmpget -v {{version}} -c {{community}} -O f {{ip}} {{oid}}"
    }
  ]
}