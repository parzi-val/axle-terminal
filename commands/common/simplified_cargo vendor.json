{
  "command": "cargo vendor",
  "description": "Vendor all dependencies of a project into the specified directory (default: vendor). More information: https://doc.rust-lang.org/cargo/commands/cargo-vendor.html.",
  "useCases": [
    {
      "description": "Vendor dependencies and configure cargo to use the vendored sources in the current project:",
      "syntax": "cargo vendor {{path/to/directory}} &gt; .cargo/config.toml"
    }
  ]
}