{
  "command": "go install",
  "description": "Compile and install packages named by the import paths. More information: https://pkg.go.dev/cmd/go#hdr-Compile_and_install_packages_and_dependencies.",
  "useCases": [
    {
      "description": "Compile and install the current package:",
      "syntax": "go install"
    },
    {
      "description": "Compile and install a specific local package:",
      "syntax": "go install {{path/to/package}}"
    },
    {
      "description": "Install the latest version of a program, ignoring go.mod in the current directory:",
      "syntax": "go install {{golang.org/x/tools/gopls}}@{{latest}}"
    },
    {
      "description": "Install a program at the version selected by go.mod in the current directory:",
      "syntax": "go install {{golang.org/x/tools/gopls}}"
    }
  ]
}