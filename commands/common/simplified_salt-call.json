{
  "command": "salt-call",
  "description": "Invoke salt locally on a salt minion. More information: https://docs.saltproject.io/en/latest/ref/cli/salt-call.html.",
  "useCases": [
    {
      "description": "Perform a highstate on this minion:",
      "syntax": "salt-call state.highstate"
    },
    {
      "description": "Perform a highstate dry-run, compute all changes but don't actually perform them:",
      "syntax": "salt-call state.highstate test=true"
    },
    {
      "description": "Perform a highstate with verbose debugging output:",
      "syntax": "salt-call -l debug state.highstate"
    },
    {
      "description": "List this minion's grains:",
      "syntax": "salt-call grains.items"
    }
  ]
}