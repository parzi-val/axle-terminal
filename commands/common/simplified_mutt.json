{
  "command": "mutt",
  "description": "Command-line email client. More information: http://mutt.org.",
  "useCases": [
    {
      "description": "Open the specified mailbox:",
      "syntax": "mutt -f {{mailbox}}"
    },
    {
      "description": "Send an email and specify a subject and a cc recipient:",
      "syntax": "mutt -s {{subject}} -c {{cc@example.com}} {{recipient@example.com}}"
    },
    {
      "description": "Send an email with files attached:",
      "syntax": "mutt -a {{file1}} {{file2}} -- {{recipient@example.com}}"
    },
    {
      "description": "Specify a file to include as the message body:",
      "syntax": "mutt -i {{path/to/file}} {{recipient@example.com}}"
    },
    {
      "description": "Specify a draft file containing the header and the body of the message, in RFC 5322 format:",
      "syntax": "mutt -H {{path/to/file}} {{recipient@example.com}}"
    }
  ]
}