{
  "command": "fmt",
  "description": "Reformat a text file by joining its paragraphs and limiting the line width to a number of characters (75 by default). More information: https://www.gnu.org/software/coreutils/manual/html_node/fmt-invocation.html.",
  "useCases": [
    {
      "description": "Reformat a file:",
      "syntax": "fmt {{path/to/file}}"
    },
    {
      "description": "Reformat a file producing output lines of (at most) n characters:",
      "syntax": "fmt -w {{n}} {{path/to/file}}"
    },
    {
      "description": "Reformat a file without joining lines shorter than the given width together:",
      "syntax": "fmt -s {{path/to/file}}"
    },
    {
      "description": "Reformat a file with uniform spacing (1 space between words and 2 spaces between paragraphs):",
      "syntax": "fmt -u {{path/to/file}}"
    }
  ]
}