{
  "command": "docker node",
  "description": "Manage Docker Swarm nodes. More information: https://docs.docker.com/reference/cli/docker/node/.",
  "useCases": [
    {
      "description": "List nodes in the swarm:",
      "syntax": "docker node ls"
    },
    {
      "description": "List tasks running on one or more nodes, defaults to the current node:",
      "syntax": "docker node ps {{node1 node2 node3 ...}}"
    },
    {
      "description": "Display detailed information on one or more nodes:",
      "syntax": "docker node inspect {{node1 node2 node3 ...}}"
    },
    {
      "description": "Promote one or more nodes to manager in the swarm:",
      "syntax": "docker node promote {{node1 node2 node3 ...}}"
    },
    {
      "description": "Demote one or more nodes from manager in the swarm:",
      "syntax": "docker node demote {{node1 node2 node3 ...}}"
    },
    {
      "description": "Remove one or more nodes from the swarm:",
      "syntax": "docker node rm {{node1 node2 node3 ...}}"
    },
    {
      "description": "Update metadata about a node, such as its availability, labels, or roles:",
      "syntax": "docker node update --{{availability|role|label-add|...}} {{active|worker|foo|...}} {{node1}}"
    }
  ]
}