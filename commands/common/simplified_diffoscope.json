{
  "command": "diffoscope",
  "description": "Compare files, archives, and directories. More information: https://diffoscope.org.",
  "useCases": [
    {
      "description": "Compare two files:",
      "syntax": "diffoscope {{path/to/file1}} {{path/to/file2}}"
    },
    {
      "description": "Compare two files without displaying a progress bar:",
      "syntax": "diffoscope --no-progress {{path/to/file1}} {{path/to/file2}}"
    },
    {
      "description": "Compare two files and write an HTML-report to a file (use - for stdout):",
      "syntax": "diffoscope --html {{path/to/outfile|-}} {{path/to/file1}} {{path/to/file2}}"
    },
    {
      "description": "Compare two directories excluding files with a name matching a specified pattern:",
      "syntax": "diffoscope --exclude {{pattern}} {{path/to/directory1}} {{path/to/directory2}}"
    },
    {
      "description": "Compare two directories and control whether directory metadata is considered:",
      "syntax": "diffoscope --exclude-directory-metadata {{auto|yes|no|recursive}} {{path/to/directory1}} {{path/to/directory2}}"
    }
  ]
}