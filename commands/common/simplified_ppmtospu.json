{
  "command": "ppmtospu",
  "description": "Convert a PPM file to an Atari Spectrum 512 image. More information: https://netpbm.sourceforge.net/doc/ppmtospu.html.",
  "useCases": [
    {
      "description": "Convert a PPM file to an Atari Spectrum 512 image:",
      "syntax": "ppmtospu {{path/to/input.ppm}} &gt; {{path/to/output.spu}}"
    },
    {
      "description": "Use a dithering matrix of the specified size (0 means no dithering):",
      "syntax": "ppmtospu -d{{0|2|4}} {{path/to/input.ppm}} &gt; {{path/to/output.spu}}"
    }
  ]
}