{
  "command": "fgrep",
  "description": "Matches fixed strings in files. Equivalent to grep -F. More information: https://www.gnu.org/software/grep/manual/grep.html.",
  "useCases": [
    {
      "description": "Search for an exact string in a file:",
      "syntax": "fgrep {{search_string}} {{path/to/file}}"
    },
    {
      "description": "Search only lines that match entirely in one or more files:",
      "syntax": "fgrep -x {{search_string}} {{path/to/file1 path/to/file2 ...}}"
    },
    {
      "description": "Count the number of lines that match the given string in a file:",
      "syntax": "fgrep -c {{search_string}} {{path/to/file}}"
    },
    {
      "description": "Show the line number in the file along with the line matched:",
      "syntax": "fgrep -n {{search_string}} {{path/to/file}}"
    },
    {
      "description": "Display all lines except those that contain the search string:",
      "syntax": "fgrep -v {{search_string}} {{path/to/file}}"
    },
    {
      "description": "Display filenames whose content matches the search string at least once:",
      "syntax": "fgrep -l {{search_string}} {{path/to/file1 path/to/file2 ...}}"
    }
  ]
}