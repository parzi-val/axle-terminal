{
  "command": "patch",
  "description": "Patch a file (or files) with a diff file. Note that diff files should be generated by the diff command. More information: https://manned.org/patch.",
  "useCases": [
    {
      "description": "Apply a patch using a diff file (filenames must be included in the diff file):",
      "syntax": "patch &lt; {{patch.diff}}"
    },
    {
      "description": "Apply a patch to a specific file:",
      "syntax": "patch {{path/to/file}} &lt; {{patch.diff}}"
    },
    {
      "description": "Patch a file writing the result to a different file:",
      "syntax": "patch {{path/to/input_file}} -o {{path/to/output_file}} &lt; {{patch.diff}}"
    },
    {
      "description": "Apply a patch to the current directory:",
      "syntax": "patch -p1 &lt; {{patch.diff}}"
    },
    {
      "description": "Apply the reverse of a patch:",
      "syntax": "patch -R &lt; {{patch.diff}}"
    }
  ]
}