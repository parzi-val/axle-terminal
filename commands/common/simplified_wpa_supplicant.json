{
  "command": "wpa_supplicant",
  "description": "Manage protected wireless networks. More information: https://manned.org/wpa_supplicant.1.",
  "useCases": [
    {
      "description": "Join a protected wireless network:",
      "syntax": "wpa_supplicant -i {{interface}} -c {{path/to/wpa_supplicant_conf.conf}}"
    },
    {
      "description": "Join a protected wireless network and run it in a daemon:",
      "syntax": "wpa_supplicant -B -i {{interface}} -c {{path/to/wpa_supplicant_conf.conf}}"
    }
  ]
}