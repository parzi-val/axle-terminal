{
  "command": "doctl databases db",
  "description": "Manage databases that are served by a database cluster. More information: https://docs.digitalocean.com/reference/doctl/reference/databases/db.",
  "useCases": [
    {
      "description": "Run a doctl databases db command with an access token:",
      "syntax": "doctl databases db {{command}} --access-token {{access_token}}"
    },
    {
      "description": "Retrieve the name of the given database hosted in the given database cluster:",
      "syntax": "doctl databases db get {{database_id}} {{database_name}}"
    },
    {
      "description": "List existing databases hosted within a given database cluster:",
      "syntax": "doctl databases db list {{database_id}}"
    },
    {
      "description": "Create a database with the given name in the given database cluster:",
      "syntax": "doctl databases db create {{database_id}} {{database_name}}"
    },
    {
      "description": "Delete the database with the given name in the given database cluster:",
      "syntax": "doctl databases db delete {{database_id}} {{database_name}}"
    }
  ]
}