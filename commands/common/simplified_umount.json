{
  "command": "umount",
  "description": "Unlink a filesystem from its mount point, making it no longer accessible. A filesystem cannot be unmounted when it is busy. More information: https://man.openbsd.org/umount.",
  "useCases": [
    {
      "description": "Unmount a filesystem, by passing the path to the source it is mounted from:",
      "syntax": "umount {{path/to/device_file}}"
    },
    {
      "description": "Unmount a filesystem, by passing the path to the target where it is mounted:",
      "syntax": "umount {{path/to/mounted_directory}}"
    },
    {
      "description": "Unmount all mounted filesystems (except the proc filesystem):",
      "syntax": "umount -a"
    }
  ]
}