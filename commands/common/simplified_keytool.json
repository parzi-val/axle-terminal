{
  "command": "keytool",
  "description": "A certificate management utility included with Java. More information: https://docs.oracle.com/en/java/javase/20/docs/specs/man/keytool.html.",
  "useCases": [
    {
      "description": "Create a keystore:",
      "syntax": "keytool -genkeypair -v -keystore {{path/to/file.keystore}} -alias {{key_name}}"
    },
    {
      "description": "Change a keystore password:",
      "syntax": "keytool -storepasswd -keystore {{path/to/file.keystore}}"
    },
    {
      "description": "Change a key's password inside a specific keystore:",
      "syntax": "keytool -keypasswd -alias {{key_name}} -keystore {{path/to/file.keystore}}"
    }
  ]
}