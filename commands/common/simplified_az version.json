{
  "command": "az version",
  "description": "Shows the current version of Azure CLI modules and extensions. Part of azure-cli (also known as az). More information: https://learn.microsoft.com/cli/azure/reference-index?view=azure-cli-latest#az-version.",
  "useCases": [
    {
      "description": "Show the current version of Azure CLI modules and extensions in JSON format:",
      "syntax": "az version"
    },
    {
      "description": "Show the current version of Azure CLI modules and extensions in a given format:",
      "syntax": "az version --output {{json|table|tsv}}"
    }
  ]
}