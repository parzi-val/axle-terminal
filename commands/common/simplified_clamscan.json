{
  "command": "clamscan",
  "description": "A command-line virus scanner. More information: https://docs.clamav.net/manual/Usage/Scanning.html#clamscan.",
  "useCases": [
    {
      "description": "Scan a file for vulnerabilities:",
      "syntax": "clamscan {{path/to/file}}"
    },
    {
      "description": "Scan all files recursively in a specific directory:",
      "syntax": "clamscan -r {{path/to/directory}}"
    },
    {
      "description": "Scan data from stdin:",
      "syntax": "{{command}} | clamscan -"
    },
    {
      "description": "Specify a virus database file or directory of files:",
      "syntax": "clamscan --database {{path/to/database_file_or_directory}}"
    },
    {
      "description": "Scan the current directory and output only infected files:",
      "syntax": "clamscan --infected"
    },
    {
      "description": "Print the scan report to a log file:",
      "syntax": "clamscan --log {{path/to/log_file}}"
    },
    {
      "description": "Move infected files to a specific directory:",
      "syntax": "clamscan --move {{path/to/quarantine_directory}}"
    },
    {
      "description": "Remove infected files:",
      "syntax": "clamscan --remove yes"
    }
  ]
}