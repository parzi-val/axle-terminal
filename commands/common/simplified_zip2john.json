{
  "command": "zip2john",
  "description": "Extract password hashes from Zip archives for use with John the Ripper password cracker. This is a utility tool usually installed as part of the John the Ripper installation. More information: https://www.openwall.com/john/.",
  "useCases": [
    {
      "description": "Extract the password hash from an archive, listing all files in the archive:",
      "syntax": "zip2john {{path/to/file.zip}}"
    },
    {
      "description": "Extract the password hash using [o]nly a specific compressed file:",
      "syntax": "zip2john -o {{path/to/compressed_file}} {{path/to/file.zip}}"
    },
    {
      "description": "Extract the password hash from a compressed file to a specific file (for use with John the Ripper):",
      "syntax": "zip2john -o {{path/to/compressed_file}} {{path/to/file.zip}} &gt; {{file.hash}}"
    }
  ]
}