{
  "command": "phpcpd",
  "description": "A copy and paste detector for PHP code. More information: https://github.com/sebastianbergmann/phpcpd.",
  "useCases": [
    {
      "description": "Analyze duplicated code for a specific file or directory:",
      "syntax": "phpcpd {{path/to/file_or_directory}}"
    },
    {
      "description": "Analyze using fuzzy matching for variable names:",
      "syntax": "phpcpd --fuzzy {{path/to/file_or_directory}}"
    },
    {
      "description": "Specify a minimum number of identical lines (defaults to 5):",
      "syntax": "phpcpd --min-lines {{number_of_lines}} {{path/to/file_or_directory}}"
    },
    {
      "description": "Specify a minimum number of identical tokens (defaults to 70):",
      "syntax": "phpcpd --min-tokens {{number_of_tokens}} {{path/to/file_or_directory}}"
    },
    {
      "description": "Exclude a directory from analysis (must be relative to the source):",
      "syntax": "phpcpd --exclude {{path/to/excluded_directory}} {{path/to/file_or_directory}}"
    },
    {
      "description": "Output the results to a PHP-CPD XML file:",
      "syntax": "phpcpd --log-pmd {{path/to/log_file}} {{path/to/file_or_directory}}"
    }
  ]
}