{
  "command": "gitlint",
  "description": "Git commit message linter checks your commit messages for style. More information: https://jorisroovers.com/gitlint/.",
  "useCases": [
    {
      "description": "Check the last commit message:",
      "syntax": "gitlint"
    },
    {
      "description": "The range of commits to lint:",
      "syntax": "gitlint --commits {{single_refspec_argument}}"
    },
    {
      "description": "Path to a directory or Python module with extra user-defined rules:",
      "syntax": "gitlint --extra-path {{path/to/directory}}"
    },
    {
      "description": "Start a specific CI job:",
      "syntax": "gitlint --target {{path/to/target_directory}}"
    },
    {
      "description": "Path to a file containing a commit-msg:",
      "syntax": "gitlint --msg-filename {{path/to/filename}}"
    },
    {
      "description": "Read staged commit meta-info from the local repository:",
      "syntax": "gitlint --staged"
    }
  ]
}