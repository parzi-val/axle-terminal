{
  "command": "pio ci",
  "description": "Build PlatformIO projects with an arbitrary source code structure. This will create a new temporary project which the source code will be copied into. More information: https://docs.platformio.org/en/latest/core/userguide/cmd_ci.html.",
  "useCases": [
    {
      "description": "Build a PlatformIO project in the default system temporary directory and delete it afterwards:",
      "syntax": "pio ci {{path/to/project}}"
    },
    {
      "description": "Build a PlatformIO project and specify specific libraries:",
      "syntax": "pio ci --lib {{path/to/library_directory}} {{path/to/project}}"
    },
    {
      "description": "Build a PlatformIO project and specify a specific board (pio boards lists all of them):",
      "syntax": "pio ci --board {{board}} {{path/to/project}}"
    },
    {
      "description": "Build a PlatformIO project in a specific directory:",
      "syntax": "pio ci --build-dir {{path/to/build_directory}} {{path/to/project}}"
    },
    {
      "description": "Build a PlatformIO project and don't delete the build directory:",
      "syntax": "pio ci --keep-build-dir {{path/to/project}}"
    },
    {
      "description": "Build a PlatformIO project using a specific configuration file:",
      "syntax": "pio ci --project-conf {{path/to/platformio.ini}}"
    }
  ]
}