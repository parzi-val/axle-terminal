{
  "command": "vboxmanage movevm",
  "description": "Move a virtual machine (VM) to a new location on the host system. More information: https://www.virtualbox.org/manual/ch08.html#vboxmanage-movevm.",
  "useCases": [
    {
      "description": "Move the specified virtual machine to the current location:",
      "syntax": "VBoxManage movevm {{vm_name}}"
    },
    {
      "description": "Specify the new location (full or relative pathname) of the virtual machine:",
      "syntax": "VBoxManage movevm {{vm_name}} --folder {{path/to/new_location}}"
    }
  ]
}