{
  "command": "ar",
  "description": "Create, modify, and extract from Unix archives. Typically used for static libraries (.a) and Debian packages (.deb). See also: tar. More information: https://manned.org/ar.",
  "useCases": [
    {
      "description": "E[x]tract all members from an archive:",
      "syntax": "ar x {{path/to/file.a}}"
    },
    {
      "description": "Lis[t] contents in a specific archive:",
      "syntax": "ar t {{path/to/file.ar}}"
    },
    {
      "description": "[r]eplace or add specific files to an archive:",
      "syntax": "ar r {{path/to/file.deb}} {{path/to/debian-binary path/to/control.tar.gz path/to/data.tar.xz ...}}"
    },
    {
      "description": "In[s]ert an object file index (equivalent to using ranlib):",
      "syntax": "ar s {{path/to/file.a}}"
    },
    {
      "description": "Create an archive with specific files and an accompanying object file index:",
      "syntax": "ar rs {{path/to/file.a}} {{path/to/file1.o path/to/file2.o ...}}"
    }
  ]
}