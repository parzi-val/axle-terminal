{
  "command": "esptool.py",
  "description": "Bootloader utility for Espressif chips (e.g. ESP8266). More information: https://docs.espressif.com/projects/esptool/en/latest/esp32/.",
  "useCases": [
    {
      "description": "Flash a firmware file to an ESP chip with a given port and baud rate:",
      "syntax": "sudo esptool.py --port {{port}} --baud {{baud_rate}} write_flash 0x0 {{path/to/firmware.bin}}"
    },
    {
      "description": "Clear the flash of an ESP chip:",
      "syntax": "sudo esptool.py --port {{port}} --baud {{baud_rate}} erase_flash"
    }
  ]
}