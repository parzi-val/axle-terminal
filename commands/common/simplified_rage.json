{
  "command": "rage",
  "description": "A simple, secure and modern file encryption tool (and Rust library) with small explicit keys, no config options, and UNIX-style composability. Rust implementation of age. More information: https://github.com/str4d/rage.",
  "useCases": [
    {
      "description": "Encrypt a file for user and save it to message.age:",
      "syntax": "echo &quot;{{Your secret message}}&quot; | rage --encrypt --recipient {{user}} --output {{path/to/message.age}}"
    },
    {
      "description": "Decrypt a file with identity_file and save it to message:",
      "syntax": "rage --decrypt --identity {{path/to/identity_file}} --output {{message}}"
    }
  ]
}