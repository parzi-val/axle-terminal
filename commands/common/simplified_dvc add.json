{
  "command": "dvc add",
  "description": "Add changed files to the index. More information: https://dvc.org/doc/command-reference/add.",
  "useCases": [
    {
      "description": "Add a single target file to the index:",
      "syntax": "dvc add {{path/to/file}}"
    },
    {
      "description": "Add a target directory to the index:",
      "syntax": "dvc add {{path/to/directory}}"
    },
    {
      "description": "Recursively add all the files in a given target directory:",
      "syntax": "dvc add --recursive {{path/to/directory}}"
    },
    {
      "description": "Add a target file with a custom .dvc filename:",
      "syntax": "dvc add --file {{custom_name.dvc}} {{path/to/file}}"
    }
  ]
}