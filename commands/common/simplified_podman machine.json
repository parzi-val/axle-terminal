{
  "command": "podman machine",
  "description": "Create and manage virtual machines running Podman. Included with Podman version 4 or greater. More information: https://docs.podman.io/en/latest/markdown/podman-machine.1.html.",
  "useCases": [
    {
      "description": "List existing machines:",
      "syntax": "podman machine ls"
    },
    {
      "description": "Create a new default machine:",
      "syntax": "podman machine init"
    },
    {
      "description": "Create a new machine with a specific name:",
      "syntax": "podman machine init {{name}}"
    },
    {
      "description": "Create a new machine with different resources:",
      "syntax": "podman machine init --cpus={{4}} --memory={{4096}} --disk-size={{50}}"
    },
    {
      "description": "Start or stop a machine:",
      "syntax": "podman machine {{start|stop}} {{name}}"
    },
    {
      "description": "Connect to a running machine via SSH:",
      "syntax": "podman machine ssh {{name}}"
    },
    {
      "description": "Inspect information about a machine:",
      "syntax": "podman machine inspect {{name}}"
    }
  ]
}