{
  "command": "csvkit",
  "description": "Manipulation toolkit for CSV files. See also: csvclean, csvcut, csvformat, csvgrep, csvlook, csvpy, csvsort, csvstat. More information: https://csvkit.readthedocs.io/en/0.9.1/cli.html.",
  "useCases": [
    {
      "description": "Run a command on a CSV file with a custom delimiter:",
      "syntax": "{{command}} -d {{delimiter}} {{path/to/file.csv}}"
    },
    {
      "description": "Run a command on a CSV file with a tab as a delimiter (overrides -d):",
      "syntax": "{{command}} -t {{path/to/file.csv}}"
    },
    {
      "description": "Run a command on a CSV file with a custom quote character:",
      "syntax": "{{command}} -q {{quote_char}} {{path/to/file.csv}}"
    },
    {
      "description": "Run a command on a CSV file with no header row:",
      "syntax": "{{command}} -H {{path/to/file.csv}}"
    }
  ]
}