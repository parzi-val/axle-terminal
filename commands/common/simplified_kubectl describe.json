{
  "command": "kubectl describe",
  "description": "Show details of Kubernetes objects and resources. More information: https://kubernetes.io/docs/reference/generated/kubectl/kubectl-commands#describe.",
  "useCases": [
    {
      "description": "Show details of pods in a [n]amespace:",
      "syntax": "kubectl describe pods --namespace {{namespace}}"
    },
    {
      "description": "Show details of nodes in a [n]amespace:",
      "syntax": "kubectl describe nodes --namespace {{namespace}}"
    },
    {
      "description": "Show the details of a specific pod in a [n]amespace:",
      "syntax": "kubectl describe pods {{pod_name}} --namespace {{namespace}}"
    },
    {
      "description": "Show the details of a specific node in a [n]amespace:",
      "syntax": "kubectl describe nodes {{node_name}} --namespace {{namespace}}"
    },
    {
      "description": "Show details of Kubernetes objects defined in a YAML manifest [f]ile:",
      "syntax": "kubectl describe --file {{path/to/manifest.yaml}}"
    }
  ]
}