{
  "command": "cargo check",
  "description": "Check a local package and all of its dependencies for errors. More information: https://doc.rust-lang.org/cargo/commands/cargo-check.html.",
  "useCases": [
    {
      "description": "Check the current package:",
      "syntax": "cargo check"
    },
    {
      "description": "Check all tests:",
      "syntax": "cargo check --tests"
    },
    {
      "description": "Check the integration tests in tests/integration_test1.rs:",
      "syntax": "cargo check --test {{integration_test1}}"
    },
    {
      "description": "Check the current package with the features feature1 and feature2:",
      "syntax": "cargo check --features {{feature1,feature2}}"
    },
    {
      "description": "Check the current package with default features disabled:",
      "syntax": "cargo check --no-default-features"
    }
  ]
}