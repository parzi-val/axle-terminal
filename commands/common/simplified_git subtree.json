{
  "command": "git subtree",
  "description": "Manage project dependencies as subprojects. More information: https://manpages.debian.org/latest/git-man/git-subtree.1.html.",
  "useCases": [
    {
      "description": "Add a Git repository as a subtree:",
      "syntax": "git subtree add --prefix={{path/to/directory/}} --squash {{repository_url}} {{branch_name}}"
    },
    {
      "description": "Update subtree repository to its latest commit:",
      "syntax": "git subtree pull --prefix={{path/to/directory/}} {{repository_url}} {{branch_name}}"
    },
    {
      "description": "Merge recent changes up to the latest subtree commit into the subtree:",
      "syntax": "git subtree merge --prefix={{path/to/directory/}} --squash {{repository_url}} {{branch_name}}"
    },
    {
      "description": "Push commits to a subtree repository:",
      "syntax": "git subtree push --prefix={{path/to/directory/}} {{repository_url}} {{branch_name}}"
    },
    {
      "description": "Extract a new project history from the history of a subtree:",
      "syntax": "git subtree split --prefix={{path/to/directory/}} {{repository_url}} -b {{branch_name}}"
    }
  ]
}