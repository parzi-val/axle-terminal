{
  "command": "git var",
  "description": "Print a Git logical variable's value. See git config, which is preferred over git var. More information: https://git-scm.com/docs/git-var.",
  "useCases": [
    {
      "description": "Print the value of a Git logical variable:",
      "syntax": "git var {{GIT_AUTHOR_IDENT|GIT_COMMITTER_IDENT|GIT_EDITOR|GIT_PAGER}}"
    },
    {
      "description": "[l]ist all Git logical variables:",
      "syntax": "git var -l"
    }
  ]
}