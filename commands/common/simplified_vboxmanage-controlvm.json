{
  "command": "vboxmanage-controlvm",
  "description": "Change the state and the settings of a currently running virtual machine. More information: https://www.virtualbox.org/manual/ch08.html#vboxmanage-controlvm.",
  "useCases": [
    {
      "description": "Temporarily stop the execution of a virtual machine:",
      "syntax": "VBoxManage controlvm {{uuid|vm_name}} pause"
    },
    {
      "description": "Resume the execution of a paused virtual machine:",
      "syntax": "VBoxManage controlvm {{uuid|vm_name}} resume"
    },
    {
      "description": "Perform a cold reset on the virtual machine:",
      "syntax": "VBoxManage controlvm {{uuid|vm_name}} reset"
    },
    {
      "description": "Poweroff a virtual machine with the same effect as pulling the power cable of a computer:",
      "syntax": "VBoxManage controlvm {{uuid|vm_name}} poweroff"
    },
    {
      "description": "Shutdown the virtual machine and save its current state:",
      "syntax": "VBoxManage controlvm {{uuid|vm_name}} savestate"
    },
    {
      "description": "Send an ACPI (Advanced Configuration and Power Interface) shutdown signal to the virtual machine:",
      "syntax": "VBoxManage controlvm {{uuid|vm_name}} acpipowerbutton"
    },
    {
      "description": "Send command to reboot itself to the guest OS:",
      "syntax": "VBoxManage controlvm {{uuid|vm_name}} reboot"
    },
    {
      "description": "Shutdown down the virtual machine without saving its state:",
      "syntax": "VBoxManage controlvm {{uuid|vm_name}} shutdown"
    }
  ]
}