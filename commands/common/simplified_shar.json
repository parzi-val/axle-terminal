{
  "command": "shar",
  "description": "Create a shell archive. More information: https://manned.org/shar.",
  "useCases": [
    {
      "description": "Create a shell script that when executed extracts the given files from itself:",
      "syntax": "shar {{path/to/file1 path/to/file2 ...}} &gt; {{path/to/archive.sh}}"
    }
  ]
}