{
  "command": "tlmgr gui",
  "description": "Start a graphical user interface for tlmgr. tlmgr gui depends on the package perl-tk, which has to be installed manually. More information: https://www.tug.org/texlive/tlmgr.html.",
  "useCases": [
    {
      "description": "Start a GUI for tlmgr:",
      "syntax": "sudo tlmgr gui"
    },
    {
      "description": "Start a GUI specifying the background color:",
      "syntax": "sudo tlmgr gui -background &quot;{{#f39bc3}}&quot;"
    },
    {
      "description": "Start a GUI specifying the foreground color:",
      "syntax": "sudo tlmgr gui -foreground &quot;{{#0ef3bd}}&quot;"
    },
    {
      "description": "Start a GUI specifying the font and font size:",
      "syntax": "sudo tlmgr gui -font &quot;{{helvetica 18}}&quot;"
    },
    {
      "description": "Start a GUI setting a specific geometry:",
      "syntax": "sudo tlmgr gui -geometry {{width}}x{{height}}-{{xpos}}+{{ypos}}"
    },
    {
      "description": "Start a GUI passing an arbitrary X resource string:",
      "syntax": "sudo tlmgr gui -xrm {{xresource}}"
    }
  ]
}