{
  "command": "stow",
  "description": "Symlink manager. Often used to manage dotfiles. See also: chezmoi, tuckr, vcsh, homeshick. More information: https://www.gnu.org/software/stow.",
  "useCases": [
    {
      "description": "Symlink all files recursively to a given directory:",
      "syntax": "stow --target={{path/to/target_directory}} {{file1 directory1 file2 directory2}}"
    },
    {
      "description": "Delete symlinks recursively from a given directory:",
      "syntax": "stow --delete --target={{path/to/target_directory}} {{file1 directory1 file2 directory2}}"
    },
    {
      "description": "Simulate to see what the result would be like:",
      "syntax": "stow --simulate --target={{path/to/target_directory}} {{file1 directory1 file2 directory2}}"
    },
    {
      "description": "Delete and resymlink:",
      "syntax": "stow --restow --target={{path/to/target_directory}} {{file1 directory1 file2 directory2}}"
    },
    {
      "description": "Exclude files matching a regular expression:",
      "syntax": "stow --ignore={{regular_expression}} --target={{path/to/target_directory}} {{file1 directory1 file2 directory2}}"
    }
  ]
}