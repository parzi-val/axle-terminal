{
  "command": "npm login",
  "description": "Log in to a registry user account. See also: npm logout for logging out. More information: https://docs.npmjs.com/cli/commands/npm-login.",
  "useCases": [
    {
      "description": "Log in to a registry user account and save the credentials to the .npmrc file:",
      "syntax": "npm login"
    },
    {
      "description": "Log in using a custom registry:",
      "syntax": "npm login --registry={{registry_url}}"
    },
    {
      "description": "Log in using a specific authentication strategy:",
      "syntax": "npm login --auth-type={{legacy|web}}"
    }
  ]
}