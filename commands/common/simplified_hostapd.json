{
  "command": "hostapd",
  "description": "Start an access point using a wireless interface. More information: https://w1.fi/hostapd/.",
  "useCases": [
    {
      "description": "Start an access point:",
      "syntax": "sudo hostapd {{path/to/hostapd.conf}}"
    },
    {
      "description": "Start an access point, forking into the background:",
      "syntax": "sudo hostapd -B {{path/to/hostapd.conf}}"
    }
  ]
}