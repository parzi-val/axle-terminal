{
  "command": "pulumi config",
  "description": "Manage configuration of a Pulumi stack. More information: https://www.pulumi.com/docs/iac/cli/commands/pulumi_config/.",
  "useCases": [
    {
      "description": "View current configuration in JSON format:",
      "syntax": "pulumi config --json"
    },
    {
      "description": "View configuration for a specified stack:",
      "syntax": "pulumi config --stack {{stack_name}}"
    },
    {
      "description": "Get the value of a configuration key:",
      "syntax": "pulumi config get {{key}}"
    },
    {
      "description": "Remove a configuration value:",
      "syntax": "pulumi config rm {{key}}"
    },
    {
      "description": "Set a value for a configuration key from a file:",
      "syntax": "cat {{path/to/file}} | pulumi config set {{key}}"
    },
    {
      "description": "Set a secret value (e.g. API key) for a configuration key and store/display as ciphertext:",
      "syntax": "pulumi config set --secret {{key}} {{S3cr37_value}}"
    },
    {
      "description": "Remove multiple configuration values from a specified configuration file:",
      "syntax": "pulumi config --config-file {{path/to/file}} rm-all {{key1 key2 ...}}"
    }
  ]
}