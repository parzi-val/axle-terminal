{
  "command": "proxify",
  "description": "A versatile and portable proxy for capturing, manipulating, and replaying HTTP/HTTPS traffic on the go. See also: mitmproxy. More information: https://github.com/projectdiscovery/proxify.",
  "useCases": [
    {
      "description": "Start a HTTP proxy (on the loopback network interface 127.0.0.1 and port 8888):",
      "syntax": "proxify"
    },
    {
      "description": "Start a HTTP proxy on a custom network interface and port (may require sudo for a port number lower than 1024):",
      "syntax": "proxify -http-addr &quot;{{ip_address}}:{{port_number}}&quot;"
    },
    {
      "description": "Specify output format and output file:",
      "syntax": "proxify -output-format {{jsonl|yaml}} -output {{path/to/file}}"
    },
    {
      "description": "Display help:",
      "syntax": "proxify -h"
    }
  ]
}