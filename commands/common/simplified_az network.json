{
  "command": "az network",
  "description": "Manage Azure Network resources. Part of azure-cli (also known as az). More information: https://learn.microsoft.com/cli/azure/network.",
  "useCases": [
    {
      "description": "List network resources in a region that are used against a subscription quota:",
      "syntax": "az network list-usages"
    },
    {
      "description": "List all virtual networks in a subscription:",
      "syntax": "az network vnet list"
    },
    {
      "description": "Create a virtual network:",
      "syntax": "az network vnet create --address-prefixes {{10.0.0.0/16}} --name {{vnet}} --resource_group {{group_name}} --submet-name {{subnet}} --subnet-prefixes {{10.0.0.0/24}}"
    },
    {
      "description": "Enable accelerated networking for a network interface card:",
      "syntax": "az network nic update --accelerated-networking true --name {{nic}} --resource-group {{resource_group}}"
    }
  ]
}