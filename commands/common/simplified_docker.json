{
  "command": "docker",
  "description": "Manage Docker containers and images. Some subcommands such as run have their own usage documentation. More information: https://docs.docker.com/reference/cli/docker/.",
  "useCases": [
    {
      "description": "List all Docker containers (running and stopped):",
      "syntax": "docker ps --all"
    },
    {
      "description": "Start a container from an image, with a custom name:",
      "syntax": "docker run --name {{container_name}} {{image}}"
    },
    {
      "description": "Start or stop an existing container:",
      "syntax": "docker {{start|stop}} {{container_name}}"
    },
    {
      "description": "Pull an image from a Docker registry:",
      "syntax": "docker pull {{image}}"
    },
    {
      "description": "Display the list of already downloaded images:",
      "syntax": "docker images"
    },
    {
      "description": "Open an [i]nteractive [t]ty with Bourne shell (sh) inside a running container:",
      "syntax": "docker exec -it {{container_name}} {{sh}}"
    },
    {
      "description": "Remove stopped containers:",
      "syntax": "docker rm {{container1 container2 ...}}"
    },
    {
      "description": "Fetch and follow the logs of a container:",
      "syntax": "docker logs -f {{container_name}}"
    }
  ]
}