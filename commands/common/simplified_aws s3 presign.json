{
  "command": "aws s3 presign",
  "description": "Generate pre-signed URLs for Amazon S3 objects. More information: https://awscli.amazonaws.com/v2/documentation/api/latest/reference/s3/presign.html.",
  "useCases": [
    {
      "description": "Generate a pre-signed URL for a specific S3 object that is valid for one hour:",
      "syntax": "aws s3 presign s3://{{bucket_name}}/{{path/to/file}}"
    },
    {
      "description": "Generate a pre-signed URL valid for a specific lifetime:",
      "syntax": "aws s3 presign s3://{{bucket_name}}/{{path/to/file}} --expires-in {{duration_in_seconds}}"
    },
    {
      "description": "Display help:",
      "syntax": "aws s3 presign help"
    }
  ]
}