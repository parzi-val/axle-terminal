{
  "command": "vboxmanage-startvm",
  "description": "Start a virtual machine. More information: https://www.virtualbox.org/manual/ch08.html#vboxmanage-startvm.",
  "useCases": [
    {
      "description": "Start a virtual machine:",
      "syntax": "VBoxManage startvm {{vm_name|uuid}}"
    },
    {
      "description": "Start a virtual machine with the specified UI mode:",
      "syntax": "VBoxManage startvm {{vm_name|uuid}} --type {{headless|gui|sdl|separate}}"
    },
    {
      "description": "Specify a password file to start an encrypted virtual machine:",
      "syntax": "VBoxManage startvm {{vm_name|uuid}} --password {{path/to/password_file}}"
    },
    {
      "description": "Specify a password ID to start an encrypted virtual machine:",
      "syntax": "VBoxManage startvm {{vm_name|uuid}} --password-id {{password_id}}"
    },
    {
      "description": "Start a virtual machine with an environment variable pair name value:",
      "syntax": "VBoxManage startvm {{vm_name|uuid}} --put-env={{name}}={{value}}"
    }
  ]
}