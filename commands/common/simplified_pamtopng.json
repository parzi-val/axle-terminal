{
  "command": "pamtopng",
  "description": "Convert a PAM image to PNG. See also: pnmtopng, pngtopam. More information: https://netpbm.sourceforge.net/doc/pamtopng.html.",
  "useCases": [
    {
      "description": "Convert the specified PAM image to PNG:",
      "syntax": "pamtopng {{path/to/image.pam}} &gt; {{path/to/output.png}}"
    },
    {
      "description": "Mark the specified color as transparent in the output image:",
      "syntax": "pamtopng -transparent {{color}} {{path/to/image.pam}} &gt; {{path/to/output.png}}"
    },
    {
      "description": "Include the text in the specified file as tEXt chunks in the output:",
      "syntax": "pamtopng -text {{path/to/file.txt}} {{path/to/image.pam}} &gt; {{path/to/output.png}}"
    },
    {
      "description": "Cause the output file to be interlaced in Adam7 format:",
      "syntax": "pamtopng -interlace {{path/to/image.pam}} &gt; {{path/to/output.png}}"
    }
  ]
}