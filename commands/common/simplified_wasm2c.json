{
  "command": "wasm2c",
  "description": "Convert a file from the WebAssembly binary format to a C source file and header. More information: https://github.com/WebAssembly/wabt.",
  "useCases": [
    {
      "description": "Convert a file to a C source file and header and display it to the console:",
      "syntax": "wasm2c {{file.wasm}}"
    },
    {
      "description": "Write the output to a given file (file.h gets additionally generated):",
      "syntax": "wasm2c {{file.wasm}} -o {{file.c}}"
    }
  ]
}