{
  "command": "tbl",
  "description": "Table preprocessor for the groff (GNU Troff) document formatting system. See also groff and troff. More information: https://manned.org/tbl.",
  "useCases": [
    {
      "description": "Process input with tables, saving the output for future typesetting with groff to PostScript:",
      "syntax": "tbl {{path/to/input_file}} &gt; {{path/to/output.roff}}"
    },
    {
      "description": "Typeset input with tables to PDF using the [me] macro package:",
      "syntax": "tbl -T {{pdf}} {{path/to/input.tbl}} | groff -{{me}} -T {{pdf}} &gt; {{path/to/output.pdf}}"
    }
  ]
}