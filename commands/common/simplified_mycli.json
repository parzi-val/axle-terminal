{
  "command": "mycli",
  "description": "A command-line client for MySQL that can do auto-completion and syntax highlighting. More information: https://mycli.net.",
  "useCases": [
    {
      "description": "Connect to a local database on port 3306, using the current user's username:",
      "syntax": "mycli {{database_name}}"
    },
    {
      "description": "Connect to a database (user will be prompted for a password):",
      "syntax": "mycli -u {{username}} {{database_name}}"
    },
    {
      "description": "Connect to a database on another host:",
      "syntax": "mycli -h {{database_host}} -P {{port}} -u {{username}} {{database_name}}"
    }
  ]
}