{
  "command": "autopep8",
  "description": "Format Python code according to the PEP 8 style guide. More information: https://github.com/hhatto/autopep8.",
  "useCases": [
    {
      "description": "Format a file to stdout, with a custom maximum line length:",
      "syntax": "autopep8 {{path/to/file.py}} --max-line-length {{length}}"
    },
    {
      "description": "Format a file, displaying a diff of the changes:",
      "syntax": "autopep8 --diff {{path/to/file}}"
    },
    {
      "description": "Format a file in-place and save the changes:",
      "syntax": "autopep8 --in-place {{path/to/file.py}}"
    },
    {
      "description": "Recursively format all files in a directory in-place and save changes:",
      "syntax": "autopep8 --in-place --recursive {{path/to/directory}}"
    }
  ]
}