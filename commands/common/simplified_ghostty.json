{
  "command": "ghostty",
  "description": "A fast, feature-rich, and cross-platform terminal emulator that uses platform-native UI and GPU acceleration. Note: all options from the configuration file can also be used on the command-line (using --option=argument). More information: https://ghostty.org/docs/config/reference.",
  "useCases": [
    {
      "description": "Open a new Ghostty window (not supported on macOS):",
      "syntax": "ghostty"
    },
    {
      "description": "Run a specific command in a new Ghostty window (not supported on macOS):",
      "syntax": "ghostty -e {{command}}"
    },
    {
      "description": "List all default and configured keybindings:",
      "syntax": "ghostty +list-keybinds"
    },
    {
      "description": "List all actions (i.e. what can be triggered via keybindings):",
      "syntax": "ghostty +list-actions"
    },
    {
      "description": "Browse an interactive list of themes:",
      "syntax": "ghostty +list-themes"
    },
    {
      "description": "Print the default configuration (including comments):",
      "syntax": "ghostty +show-config --default --docs"
    }
  ]
}