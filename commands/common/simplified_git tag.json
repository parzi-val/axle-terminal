{
  "command": "git tag",
  "description": "Create, list, delete or verify tags. A tag is a static reference to a commit. More information: https://git-scm.com/docs/git-tag.",
  "useCases": [
    {
      "description": "List all tags:",
      "syntax": "git tag"
    },
    {
      "description": "Create a tag with the given name pointing to the current commit:",
      "syntax": "git tag {{tag_name}}"
    },
    {
      "description": "Create a tag with the given name pointing to a given commit:",
      "syntax": "git tag {{tag_name}} {{commit}}"
    },
    {
      "description": "Create an annotated tag with the given message:",
      "syntax": "git tag {{tag_name}} -m {{tag_message}}"
    },
    {
      "description": "Delete the tag with the given name:",
      "syntax": "git tag {{-d|--delete}} {{tag_name}}"
    },
    {
      "description": "Get updated tags from remote:",
      "syntax": "git fetch --tags"
    },
    {
      "description": "Push a tag to remote:",
      "syntax": "git push origin tag {{tag_name}}"
    },
    {
      "description": "List all tags whose ancestors include a given commit:",
      "syntax": "git tag --contains {{commit}}"
    }
  ]
}