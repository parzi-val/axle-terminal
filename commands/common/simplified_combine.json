{
  "command": "combine",
  "description": "Perform set operations on lines of two files. The order of the output lines is determined by the order of the lines in the first file. See also: diff. More information: https://joeyh.name/code/moreutils/.",
  "useCases": [
    {
      "description": "Output lines that are in both specified files:",
      "syntax": "combine {{path/to/file1}} and {{path/to/file2}}"
    },
    {
      "description": "Output lines that are in the first but not in the second file:",
      "syntax": "combine {{path/to/file1}} not {{path/to/file2}}"
    },
    {
      "description": "Output lines that in are in either of the specified files:",
      "syntax": "combine {{path/to/file1}} or {{path/to/file2}}"
    },
    {
      "description": "Output lines that are in exactly one of the specified files:",
      "syntax": "combine {{path/to/file1}} xor {{path/to/file2}}"
    }
  ]
}