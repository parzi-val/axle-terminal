{
  "command": "git apply",
  "description": "Apply a patch to files and/or to the index without creating a commit. See also git am, which applies a patch and also creates a commit. More information: https://git-scm.com/docs/git-apply.",
  "useCases": [
    {
      "description": "Print messages about the patched files:",
      "syntax": "git apply --verbose {{path/to/file}}"
    },
    {
      "description": "Apply and add the patched files to the index:",
      "syntax": "git apply --index {{path/to/file}}"
    },
    {
      "description": "Apply a remote patch file:",
      "syntax": "curl -L {{https://example.com/file.patch}} | git apply"
    },
    {
      "description": "Output diffstat for the input and apply the patch:",
      "syntax": "git apply --stat --apply {{path/to/file}}"
    },
    {
      "description": "Apply the patch in reverse:",
      "syntax": "git apply --reverse {{path/to/file}}"
    },
    {
      "description": "Store the patch result in the index without modifying the working tree:",
      "syntax": "git apply --cache {{path/to/file}}"
    }
  ]
}