{
  "command": "twopi",
  "description": "Render an image of a radial network graph from a graphviz file. Layouts: dot, neato, twopi, circo, fdp, sfdp, osage & patchwork. More information: https://graphviz.org/doc/info/command.html.",
  "useCases": [
    {
      "description": "Render a PNG image with a filename based on the input filename and output format (uppercase -O):",
      "syntax": "twopi -T {{png}} -O {{path/to/input.gv}}"
    },
    {
      "description": "Render a SVG image with the specified output filename (lowercase -o):",
      "syntax": "twopi -T {{svg}} -o {{path/to/image.svg}} {{path/to/input.gv}}"
    },
    {
      "description": "Render the output in PS, PDF, SVG, Fig, PNG, GIF, JPEG, JSON, or DOT format:",
      "syntax": "twopi -T {{format}} -O {{path/to/input.gv}}"
    },
    {
      "description": "Render a GIF image using stdin and stdout:",
      "syntax": "echo &quot;{{digraph {this -&gt; that} }}&quot; | twopi -T {{gif}} &gt; {{path/to/image.gif}}"
    },
    {
      "description": "Display help:",
      "syntax": "twopi -?"
    }
  ]
}