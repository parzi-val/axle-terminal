{
  "command": "conda create",
  "description": "Create new conda environments. More information: https://docs.conda.io/projects/conda/en/latest/commands/create.html.",
  "useCases": [
    {
      "description": "Create a new environment named py39, and install Python 3.9 and NumPy v1.11 or above in it:",
      "syntax": "conda create --yes --name {{py39}} python={{3.9}} &quot;{{numpy&gt;=1.11}}&quot;"
    },
    {
      "description": "Make exact copy of an environment:",
      "syntax": "conda create --clone {{py39}} --name {{py39-copy}}"
    },
    {
      "description": "Create a new environment with a specified name and install a given package:",
      "syntax": "conda create --name {{env_name}} {{package}}"
    }
  ]
}