{
  "command": "git switch",
  "description": "Switch between Git branches. Requires Git version 2.23+. See also git checkout. More information: https://git-scm.com/docs/git-switch.",
  "useCases": [
    {
      "description": "Switch to an existing branch:",
      "syntax": "git switch {{branch_name}}"
    },
    {
      "description": "Create a new branch and switch to it:",
      "syntax": "git switch --create {{branch_name}}"
    },
    {
      "description": "Create a new branch based on an existing commit and switch to it:",
      "syntax": "git switch --create {{branch_name}} {{commit}}"
    },
    {
      "description": "Switch to the previous branch:",
      "syntax": "git switch -"
    },
    {
      "description": "Switch to a branch and update all submodules to match:",
      "syntax": "git switch --recurse-submodules {{branch_name}}"
    },
    {
      "description": "Switch to a branch and automatically merge the current branch and any uncommitted changes into it:",
      "syntax": "git switch --merge {{branch_name}}"
    }
  ]
}