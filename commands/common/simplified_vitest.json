{
  "command": "vitest",
  "description": "Fast, modern testing framework built for Vite, offering seamless integration, TypeScript support, and a Jest-compatible API for unit, integration, and snapshot testing. More information: https://vitest.dev/guide.",
  "useCases": [
    {
      "description": "Run all available tests:",
      "syntax": "vitest run"
    },
    {
      "description": "Run the test suites from the given files:",
      "syntax": "vitest run {{path/to/file1 path/to/file2 ...}}"
    },
    {
      "description": "Run the test suites from files within the current and subdirectories, whose paths match the given regular expression:",
      "syntax": "vitest run {{regular_expression1}} {{regular_expression2}}"
    },
    {
      "description": "Run the tests whose names match the given regular expression:",
      "syntax": "vitest run --testNamePattern {{regular_expression}}"
    },
    {
      "description": "Watch files for changes and automatically re-run related tests:",
      "syntax": "vitest"
    },
    {
      "description": "Run tests with coverage:",
      "syntax": "vitest run --coverage"
    },
    {
      "description": "Run all tests but stops immediately after the first test failure:",
      "syntax": "vitest run --bail=1"
    },
    {
      "description": "Display help:",
      "syntax": "vitest --help"
    }
  ]
}