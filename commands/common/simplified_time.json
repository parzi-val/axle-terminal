{
  "command": "time",
  "description": "Measure how long a command took to run. Note: time can either exist as a shell builtin, a standalone program or both. More information: https://manned.org/time.",
  "useCases": [
    {
      "description": "Run the command and print the time measurements to stdout:",
      "syntax": "time {{command}}"
    },
    {
      "description": "Create a very simple stopwatch (only works in Bash):",
      "syntax": "time read"
    }
  ]
}