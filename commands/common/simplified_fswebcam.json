{
  "command": "fswebcam",
  "description": "Small and simple webcam for *nix. More information: https://www.sanslogic.co.uk/fswebcam.",
  "useCases": [
    {
      "description": "Take a picture:",
      "syntax": "fswebcam {{filename}}"
    },
    {
      "description": "Take a picture with custom resolution:",
      "syntax": "fswebcam -r {{width}}x{{height}} {{filename}}"
    },
    {
      "description": "Take a picture from selected device(Default is /dev/video0):",
      "syntax": "fswebcam -d {{device}} {{filename}}"
    },
    {
      "description": "Take a picture with timestamp(timestamp string is formatted by strftime):",
      "syntax": "fswebcam --timestamp {{timestamp}} {{filename}}"
    }
  ]
}