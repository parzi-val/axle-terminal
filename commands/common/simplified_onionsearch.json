{
  "command": "onionsearch",
  "description": "Scrape URLs on different .onion search engines. Note: onionsearch requires a Tor proxy running on localhost:9050; a Tor enabled browser is needed to visit the .onion websites. More information: https://github.com/megadose/OnionSearch.",
  "useCases": [
    {
      "description": "Request results from all the search engines:",
      "syntax": "onionsearch &quot;{{string}}&quot;"
    },
    {
      "description": "Request search results from specific search engines:",
      "syntax": "onionsearch &quot;{{string}}&quot; --engines {{tor66 deeplink phobos ...}}"
    },
    {
      "description": "Exclude certain search engines when searching:",
      "syntax": "onionsearch &quot;{{string}}&quot; --exclude {{candle ahmia ...}}"
    },
    {
      "description": "Limit the number of pages to load per engine:",
      "syntax": "onionsearch &quot;{{stuxnet}}&quot; --engines {{tor66 deeplink phobos ...}} --limit {{3}}"
    },
    {
      "description": "List all supported search engines:",
      "syntax": "onionsearch --help | grep -A1 -i &quot;supported engines&quot;"
    }
  ]
}