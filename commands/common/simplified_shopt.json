{
  "command": "shopt",
  "description": "Manage Bash shell options: variables (stored in $BASHOPTS) that control behavior specific to the Bash shell. Generic POSIX shell variables (stored in $SHELLOPTS) are managed with the set command instead. More information: https://www.gnu.org/software/bash/manual/html_node/The-Shopt-Builtin.html.",
  "useCases": [
    {
      "description": "List of all settable options and whether they are set:",
      "syntax": "shopt"
    },
    {
      "description": "Set an option:",
      "syntax": "shopt -s {{option_name}}"
    },
    {
      "description": "Unset an option:",
      "syntax": "shopt -u {{option_name}}"
    },
    {
      "description": "Print a list of all options and their status formatted as runnable shopt commands:",
      "syntax": "shopt -p"
    },
    {
      "description": "Display help:",
      "syntax": "help shopt"
    }
  ]
}