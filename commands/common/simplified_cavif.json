{
  "command": "cavif",
  "description": "Convert PNG/JPEG images to AVIF. Written in Rust. See also: convert. More information: https://github.com/kornelski/cavif-rs.",
  "useCases": [
    {
      "description": "Convert a JPEG file to AVIF, saving it to file.avif:",
      "syntax": "cavif {{path/to/image.jpg}}"
    },
    {
      "description": "Adjust the image quality and convert a PNG file to AVIF:",
      "syntax": "cavif --quality {{1..100}} {{path/to/image.png}}"
    },
    {
      "description": "Specify the output location:",
      "syntax": "cavif {{path/to/image.jpg}} --output {{path/to/output.avif}}"
    },
    {
      "description": "Overwrite the destination file if it already exists:",
      "syntax": "cavif --overwrite {{path/to/image.jpg}}"
    }
  ]
}