{
  "command": "accelerate",
  "description": "A library that enables the same PyTorch code to be run across any distributed configuration. More information: https://huggingface.co/docs/accelerate/index.",
  "useCases": [
    {
      "description": "Print environment information:",
      "syntax": "accelerate env"
    },
    {
      "description": "Interactively create a configuration file:",
      "syntax": "accelerate config"
    },
    {
      "description": "Print the estimated GPU memory cost of running a Hugging Face model with different data types:",
      "syntax": "accelerate estimate-memory {{name/model}}"
    },
    {
      "description": "Test an Accelerate configuration file:",
      "syntax": "accelerate test --config_file {{path/to/config.yaml}}"
    },
    {
      "description": "Run a model on CPU with Accelerate:",
      "syntax": "accelerate launch {{path/to/script.py}} {{--cpu}}"
    },
    {
      "description": "Run a model on multi-GPU with Accelerate, with 2 machines:",
      "syntax": "accelerate launch {{path/to/script.py}} --multi_gpu --num_machines 2"
    }
  ]
}