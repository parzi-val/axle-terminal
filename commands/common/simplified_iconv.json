{
  "command": "iconv",
  "description": "Convert text from one encoding to another. More information: https://manned.org/iconv.",
  "useCases": [
    {
      "description": "Convert file to a specific encoding, and print to stdout:",
      "syntax": "iconv -f {{from_encoding}} -t {{to_encoding}} {{input_file}}"
    },
    {
      "description": "Convert file to the current locale's encoding, and output to a file:",
      "syntax": "iconv -f {{from_encoding}} {{input_file}} &gt; {{output_file}}"
    },
    {
      "description": "List supported encodings:",
      "syntax": "iconv -l"
    }
  ]
}