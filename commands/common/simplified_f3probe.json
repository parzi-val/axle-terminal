{
  "command": "f3probe",
  "description": "Probe a block device (e.g. a flash drive or a microSD card) for counterfeit flash memory. See also: f3read, f3write, f3fix. More information: https://github.com/AltraMayor/f3.",
  "useCases": [
    {
      "description": "Probe a block device:",
      "syntax": "sudo f3probe {{path/to/block_device}}"
    },
    {
      "description": "Use the minimum about of RAM possible:",
      "syntax": "sudo f3probe --min-memory {{path/to/block_device}}"
    },
    {
      "description": "Time disk operations:",
      "syntax": "sudo f3probe --time-ops {{path/to/block_device}}"
    }
  ]
}