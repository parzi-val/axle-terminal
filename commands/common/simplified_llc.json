{
  "command": "llc",
  "description": "Compiles LLVM Intermediate Representation or bitcode to target-specific assembly language. More information: https://www.llvm.org/docs/CommandGuide/llc.html.",
  "useCases": [
    {
      "description": "Compile a bitcode or IR file to an assembly file with the same base name:",
      "syntax": "llc {{path/to/file.ll}}"
    },
    {
      "description": "Enable all optimizations:",
      "syntax": "llc -O3 {{path/to/input.ll}}"
    },
    {
      "description": "Output assembly to a specific file:",
      "syntax": "llc --output {{path/to/output.s}}"
    },
    {
      "description": "Emit fully relocatable, position independent code:",
      "syntax": "llc -relocation-model=pic {{path/to/input.ll}}"
    }
  ]
}