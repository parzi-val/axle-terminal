{
  "command": "bird",
  "description": "BIRD Internet Routing Daemon. Routing daemon with support for BGP, OSPF, Babel and others. More information: https://bird.network.cz/.",
  "useCases": [
    {
      "description": "Start Bird with a specific configuration file:",
      "syntax": "bird -c {{path/to/bird.conf}}"
    },
    {
      "description": "Start Bird as a specific user and group:",
      "syntax": "bird -u {{username}} -g {{group}}"
    }
  ]
}