{
  "command": "eksctl",
  "description": "The official CLI for Amazon EKS. More information: https://eksctl.io.",
  "useCases": [
    {
      "description": "Create a basic cluster:",
      "syntax": "eksctl create cluster"
    },
    {
      "description": "List the details about a cluster or all of the clusters:",
      "syntax": "eksctl get cluster --name={{name}} --region={{region}}"
    },
    {
      "description": "Create a cluster passing all configuration information in a file:",
      "syntax": "eksctl create cluster --config-file={{path/to/file}}"
    },
    {
      "description": "Create a cluster using a configuration file and skip creating nodegroups until later:",
      "syntax": "eksctl create cluster --config-file=&lt;path&gt; --without-nodegroup"
    },
    {
      "description": "Delete a cluster:",
      "syntax": "eksctl delete cluster --name={{name}} --region={{region}}"
    },
    {
      "description": "Create cluster and write cluster credentials to a file other than the default:",
      "syntax": "eksctl create cluster --name={{name}} --nodes={{4}} --kubeconfig={{path/to/config.yaml}}"
    },
    {
      "description": "Create a cluster and prevent storing cluster credentials locally:",
      "syntax": "eksctl create cluster --name={{name}} --nodes={{4}} --write-kubeconfig=false"
    },
    {
      "description": "Create a cluster and let eksctl manage cluster credentials under the ~/.kube/eksctl/clusters directory:",
      "syntax": "eksctl create cluster --name={{name}} --nodes={{4}} --auto-kubeconfig"
    }
  ]
}