{
  "command": "cargo owner",
  "description": "Manage the owners of a crate on the registry. More information: https://doc.rust-lang.org/cargo/commands/cargo-owner.html.",
  "useCases": [
    {
      "description": "Invite the given user or team as an owner:",
      "syntax": "cargo owner --add {{username|github:org_name:team_name}} {{crate}}"
    },
    {
      "description": "Remove the given user or team as an owner:",
      "syntax": "cargo owner --remove {{username|github:org_name:team_name}} {{crate}}"
    },
    {
      "description": "List owners of a crate:",
      "syntax": "cargo owner --list {{crate}}"
    },
    {
      "description": "Use the specified registry (registry names can be defined in the configuration - the default is https://crates.io):",
      "syntax": "cargo owner --registry {{name}}"
    }
  ]
}