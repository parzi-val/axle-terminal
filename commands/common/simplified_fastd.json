{
  "command": "fastd",
  "description": "VPN daemon. Works on Layer 2 or Layer 3, supports different encryption methods, used by Freifunk. See also: ivpn, mozillavpn, mullvad, warp-cli. More information: https://fastd.readthedocs.io/en/stable/.",
  "useCases": [
    {
      "description": "Start fastd with a specific configuration file:",
      "syntax": "fastd --config {{path/to/fastd.conf}}"
    },
    {
      "description": "Start a Layer 3 VPN with an MTU of 1400, loading the rest of the configuration parameters from a file:",
      "syntax": "fastd --mode {{tap}} --mtu {{1400}} --config {{path/to/fastd.conf}}"
    },
    {
      "description": "Validate a configuration file:",
      "syntax": "fastd --verify-config --config {{path/to/fastd.conf}}"
    },
    {
      "description": "Generate a new keypair:",
      "syntax": "fastd --generate-key"
    },
    {
      "description": "Show the public key to a private key in a configuration file:",
      "syntax": "fastd --show-key --config {{path/to/fastd.conf}}"
    },
    {
      "description": "Show the current version:",
      "syntax": "fastd -v"
    }
  ]
}