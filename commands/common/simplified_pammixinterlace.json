{
  "command": "pammixinterlace",
  "description": "Merge each row in an image with its two neighbours. See also: pamdeinterlace. More information: https://netpbm.sourceforge.net/doc/pammixinterlace.html.",
  "useCases": [
    {
      "description": "Merge each row in an image with its two neighbours:",
      "syntax": "pammixinterlace {{path/to/image.ppm}} &gt; {{path/to/output.ppm}}"
    },
    {
      "description": "Use the specified filtering mechanism:",
      "syntax": "pammixinterlace -filter {{linear|fir|ffmpeg}} {{path/to/image.ppm}} &gt; {{path/to/output.ppm}}"
    },
    {
      "description": "Turn on adaptive filtering mode, i.e., only modify pixels that are obviously part of a comb pattern:",
      "syntax": "pammixinterlace -adaptive {{path/to/image.ppm}} &gt; {{path/to/output.ppm}}"
    }
  ]
}