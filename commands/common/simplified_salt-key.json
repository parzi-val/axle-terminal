{
  "command": "salt-key",
  "description": "Manage salt minion keys on the salt master. Needs to be run on the salt master, likely as root or with sudo. More information: https://docs.saltproject.io/en/latest/ref/cli/salt-key.html.",
  "useCases": [
    {
      "description": "List all accepted, unaccepted and rejected minion keys:",
      "syntax": "salt-key -L"
    },
    {
      "description": "Accept a minion key by name:",
      "syntax": "salt-key -a {{MINION_ID}}"
    },
    {
      "description": "Reject a minion key by name:",
      "syntax": "salt-key -r {{MINION_ID}}"
    },
    {
      "description": "Print fingerprints of all public keys:",
      "syntax": "salt-key -F"
    }
  ]
}