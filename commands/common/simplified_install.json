{
  "command": "install",
  "description": "Copy files and set attributes. Copy files (often executable) to a system location like /usr/local/bin, give them the appropriate permissions/ownership. More information: https://www.gnu.org/software/coreutils/manual/html_node/install-invocation.html.",
  "useCases": [
    {
      "description": "Copy files to the destination:",
      "syntax": "install {{path/to/source_file1 path/to/source_file2 ...}} {{path/to/destination}}"
    },
    {
      "description": "Copy files to the destination, setting their ownership:",
      "syntax": "install --owner {{user}} {{path/to/source_file1 path/to/source_file2 ...}} {{path/to/destination}}"
    },
    {
      "description": "Copy files to the destination, setting their group ownership:",
      "syntax": "install --group {{user}} {{path/to/source_file1 path/to/source_file2 ...}} {{path/to/destination}}"
    },
    {
      "description": "Copy files to the destination, setting their mode:",
      "syntax": "install --mode {{+x}} {{path/to/source_file1 path/to/source_file2 ...}} {{path/to/destination}}"
    },
    {
      "description": "Copy files and apply access/modification times of source to the destination:",
      "syntax": "install --preserve-timestamps {{path/to/source_file1 path/to/source_file2 ...}} {{path/to/destination}}"
    },
    {
      "description": "Copy files and create the directories at the destination if they don't exist:",
      "syntax": "install -D {{path/to/source_file1 path/to/source_file2 ...}} {{path/to/destination}}"
    }
  ]
}