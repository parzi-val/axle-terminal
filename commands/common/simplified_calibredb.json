{
  "command": "calibredb",
  "description": "Manipulate an e-book database. Part of the Calibre e-book library. More information: https://manual.calibre-ebook.com/generated/en/calibredb.html.",
  "useCases": [
    {
      "description": "List e-books in the library with additional information:",
      "syntax": "calibredb list"
    },
    {
      "description": "Search for e-books displaying additional information:",
      "syntax": "calibredb list --search {{search_term}}"
    },
    {
      "description": "Search for just ids of e-books:",
      "syntax": "calibredb search {{search_term}}"
    },
    {
      "description": "Add one or more e-books to the library:",
      "syntax": "calibredb add {{path/to/file1 path/to/file2 ...}}"
    },
    {
      "description": "Recursively add all e-books under a directory to the library:",
      "syntax": "calibredb add {{-r|--recurse}} {{path/to/directory}}"
    },
    {
      "description": "Remove one or more e-books from the library. You need the e-book IDs (see above):",
      "syntax": "calibredb remove {{id1 id2 ...}}"
    }
  ]
}