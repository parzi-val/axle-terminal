{
  "command": "kubectl rollout",
  "description": "Manage the rollout of a Kubernetes resource (deployments, daemonsets, and statefulsets). More information: https://kubernetes.io/docs/reference/generated/kubectl/kubectl-commands#rollout.",
  "useCases": [
    {
      "description": "Start a rolling restart of a resource:",
      "syntax": "kubectl rollout restart {{resource_type}}/{{resource_name}}"
    },
    {
      "description": "Watch the rolling update status of a resource:",
      "syntax": "kubectl rollout status {{resource_type}}/{{resource_name}}"
    },
    {
      "description": "Roll back a resource to the previous revision:",
      "syntax": "kubectl rollout undo {{resource_type}}/{{resource_name}}"
    },
    {
      "description": "View the rollout history of a resource:",
      "syntax": "kubectl rollout history {{resource_type}}/{{resource_name}}"
    }
  ]
}