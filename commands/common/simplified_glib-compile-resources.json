{
  "command": "glib-compile-resources",
  "description": "Compiles resource files (e.g. images) into a binary resource bundle. These may be linked into GTK applications using the GResource API. More information: https://manned.org/glib-compile-resources.",
  "useCases": [
    {
      "description": "Compile resources referenced in file.gresource.xml to a .gresource binary:",
      "syntax": "glib-compile-resources {{file.gresource.xml}}"
    },
    {
      "description": "Compile resources referenced in file.gresource.xml to a C source file:",
      "syntax": "glib-compile-resources --generate-source {{file.gresource.xml}}"
    },
    {
      "description": "Compile resources in file.gresource.xml to a chosen target file, with .c, .h or .gresource extension:",
      "syntax": "glib-compile-resources --generate --target={{file.ext}} {{file.gresource.xml}}"
    },
    {
      "description": "Print a list of resource files referenced in file.gresource.xml:",
      "syntax": "glib-compile-resources --generate-dependencies {{file.gresource.xml}}"
    }
  ]
}