{
  "command": "git restore",
  "description": "Restore working tree files. Requires Git version 2.23+. See also git checkout and git reset. More information: https://git-scm.com/docs/git-restore.",
  "useCases": [
    {
      "description": "Restore an unstaged file to the version of the current commit (HEAD):",
      "syntax": "git restore {{path/to/file}}"
    },
    {
      "description": "Restore an unstaged file to the version of a specific commit:",
      "syntax": "git restore --source {{commit}} {{path/to/file}}"
    },
    {
      "description": "Discard all unstaged changes to tracked files:",
      "syntax": "git restore :/"
    },
    {
      "description": "Unstage a file:",
      "syntax": "git restore --staged {{path/to/file}}"
    },
    {
      "description": "Unstage all files:",
      "syntax": "git restore --staged :/"
    },
    {
      "description": "Discard all changes to files, both staged and unstaged:",
      "syntax": "git restore --worktree --staged :/"
    },
    {
      "description": "Interactively select sections of files to restore:",
      "syntax": "git restore --patch"
    }
  ]
}