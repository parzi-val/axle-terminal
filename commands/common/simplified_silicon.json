{
  "command": "silicon",
  "description": "Create an image of source code. More information: https://github.com/Aloxaf/silicon.",
  "useCases": [
    {
      "description": "Generate an image from a specific source file:",
      "syntax": "silicon {{path/to/source_file}} --output {{path/to/output_image}}"
    },
    {
      "description": "Generate an image from a source file with a specific programming language syntax highlighting (e.g. rust, py, js, etc.):",
      "syntax": "silicon {{path/to/source_file}} --output {{path/to/output_image}} --language {{language|extension}}"
    },
    {
      "description": "Generate an image from stdin:",
      "syntax": "{{command}} | silicon --output {{path/to/output_image}}"
    }
  ]
}