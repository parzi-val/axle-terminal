{
  "command": "lpadmin",
  "description": "Configure CUPS printers and classes. See also: lpoptions. More information: https://openprinting.github.io/cups/doc/man-lpadmin.html.",
  "useCases": [
    {
      "description": "Set the default printer:",
      "syntax": "lpadmin -d {{printer}}"
    },
    {
      "description": "Delete a specific printer or class:",
      "syntax": "lpadmin -x {{printer|class}}"
    },
    {
      "description": "Add a printer to a class:",
      "syntax": "lpadmin -p {{printer}} -c {{class}}"
    },
    {
      "description": "Remove a printer from a class:",
      "syntax": "lpadmin -p {{printer}} -r {{class}}"
    }
  ]
}