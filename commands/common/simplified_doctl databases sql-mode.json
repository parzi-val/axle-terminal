{
  "command": "doctl databases sql-mode",
  "description": "View and configure a MySQL database cluster’s global SQL modes. More information: https://docs.digitalocean.com/reference/doctl/reference/databases/sql-mode/.",
  "useCases": [
    {
      "description": "Run a doctl databases sql-mode command with an access token:",
      "syntax": "doctl databases sql-mode {{command}} --access-token {{access_token}}"
    },
    {
      "description": "Get a MySQL database cluster's SQL modes:",
      "syntax": "doctl databases sql-mode get {{database_id}}"
    },
    {
      "description": "Overwrite a MySQL database cluster's SQL modes to the specified modes:",
      "syntax": "doctl databases sql-mode set {{database_id}} {{sql_mode_1 sql_mode_2 ...}}"
    }
  ]
}