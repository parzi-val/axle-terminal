{
  "command": "csvcut",
  "description": "Filter and truncate CSV files. Like Unix's cut command, but for tabular data. Included in csvkit. More information: https://csvkit.readthedocs.io/en/latest/scripts/csvcut.html.",
  "useCases": [
    {
      "description": "Print indices and names of all columns:",
      "syntax": "csvcut -n {{data.csv}}"
    },
    {
      "description": "Extract the first and third columns:",
      "syntax": "csvcut -c {{1,3}} {{data.csv}}"
    },
    {
      "description": "Extract all columns except the fourth one:",
      "syntax": "csvcut -C {{4}} {{data.csv}}"
    },
    {
      "description": "Extract the columns named \"id\" and \"first name\" (in that order):",
      "syntax": "csvcut -c {{id,&quot;first name&quot;}} {{data.csv}}"
    }
  ]
}