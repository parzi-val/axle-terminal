{
  "command": "brew install",
  "description": "Install a Homebrew formula or cask. More information: https://docs.brew.sh/Manpage#install-options-formulacask-.",
  "useCases": [
    {
      "description": "Install a formula/cask:",
      "syntax": "brew install {{formula|cask}}"
    },
    {
      "description": "Build and install a formula from source (dependencies will still be installed from bottles):",
      "syntax": "brew install --build-from-source {{formula}}"
    },
    {
      "description": "Download the manifest, print what would be installed but don't actually install anything:",
      "syntax": "brew install --dry-run {{formula|cask}}"
    }
  ]
}