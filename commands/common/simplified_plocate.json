{
  "command": "plocate",
  "description": "Find filenames quickly. Make sure to run sudo updatedb to include new files. More information: https://plocate.sesse.net.",
  "useCases": [
    {
      "description": "Look for patterns in the database (recomputed periodically):",
      "syntax": "plocate {{pattern}}"
    },
    {
      "description": "Look for a file by its exact filename (a pattern containing no globbing characters is interpreted as *pattern*):",
      "syntax": "plocate */{{filename}}"
    }
  ]
}