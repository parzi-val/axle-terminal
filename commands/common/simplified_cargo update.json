{
  "command": "cargo update",
  "description": "Update dependencies as recorded in Cargo.lock. More information: https://doc.rust-lang.org/cargo/commands/cargo-update.html.",
  "useCases": [
    {
      "description": "Update dependencies in Cargo.lock to the latest possible version:",
      "syntax": "cargo update"
    },
    {
      "description": "Display what would be updated, but don't actually write the lockfile:",
      "syntax": "cargo update --dry-run"
    },
    {
      "description": "Update only the specified dependencies:",
      "syntax": "cargo update --package {{dependency1}} --package {{dependency2}} --package {{dependency3}}"
    },
    {
      "description": "Set a specific dependency to a specific version:",
      "syntax": "cargo update --package {{dependency}} --precise {{1.2.3}}"
    }
  ]
}