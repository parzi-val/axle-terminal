{
  "command": "loadtest",
  "description": "Run a load test on the selected HTTP or WebSockets URL. More information: https://github.com/alexfernandez/loadtest.",
  "useCases": [
    {
      "description": "Run with concurrent users and a specified amount of requests per second:",
      "syntax": "loadtest --concurrency {{10}} --rps {{200}} {{https://example.com}}"
    },
    {
      "description": "Run with a custom HTTP header:",
      "syntax": "loadtest --headers &quot;{{accept:text/plain;text-html}}&quot; {{https://example.com}}"
    },
    {
      "description": "Run with a specific HTTP method:",
      "syntax": "loadtest --method {{GET}} {{https://example.com}}"
    }
  ]
}