{
  "command": "neato",
  "description": "Render an image of a linear undirected network graph from a graphviz file. Layouts: dot, neato, twopi, circo, fdp, sfdp, osage & patchwork. More information: https://graphviz.org/doc/info/command.html.",
  "useCases": [
    {
      "description": "Render a PNG image with a filename based on the input filename and output format (uppercase -O):",
      "syntax": "neato -T {{png}} -O {{path/to/input.gv}}"
    },
    {
      "description": "Render a SVG image with the specified output filename (lowercase -o):",
      "syntax": "neato -T {{svg}} -o {{path/to/image.svg}} {{path/to/input.gv}}"
    },
    {
      "description": "Render the output in PS, PDF, SVG, Fig, PNG, GIF, JPEG, JSON, or DOT format:",
      "syntax": "neato -T {{format}} -O {{path/to/input.gv}}"
    },
    {
      "description": "Render a GIF image using stdin and stdout:",
      "syntax": "echo &quot;{{graph {this -- that} }}&quot; | neato -T {{gif}} &gt; {{path/to/image.gif}}"
    },
    {
      "description": "Display help:",
      "syntax": "neato -?"
    }
  ]
}