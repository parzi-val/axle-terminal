{
  "command": "git credential-store",
  "description": "Git helper to store passwords on disk. More information: https://git-scm.com/docs/git-credential-store.",
  "useCases": [
    {
      "description": "Store Git credentials in a specific file:",
      "syntax": "git config credential.helper 'store --file={{path/to/file}}'"
    }
  ]
}