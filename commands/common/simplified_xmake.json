{
  "command": "xmake",
  "description": "A cross-platform C & C++ build utility based on Lua. More information: https://xmake.io/#/getting_started.",
  "useCases": [
    {
      "description": "Create an Xmake C project, consisting of a hello world and xmake.lua:",
      "syntax": "xmake create --language c -P {{project_name}}"
    },
    {
      "description": "Build and run an Xmake project:",
      "syntax": "xmake build run"
    },
    {
      "description": "Run a compiled Xmake target directly:",
      "syntax": "xmake run {{target_name}}"
    },
    {
      "description": "Configure a project's build targets:",
      "syntax": "xmake config --plat={{macosx|linux|iphoneos|...}} --arch={{x86_64|i386|arm64|...}} --mode={{debug|release}}"
    },
    {
      "description": "Install the compiled target to a directory:",
      "syntax": "xmake install -o {{path/to/directory}}"
    }
  ]
}