{
  "command": "7zr",
  "description": "File archiver with a high compression ratio. Similar to 7z except that it only supports 7z files. More information: https://manned.org/7zr.",
  "useCases": [
    {
      "description": "[a]rchive a file or directory:",
      "syntax": "7zr a {{path/to/archive.7z}} {{path/to/file_or_directory}}"
    },
    {
      "description": "Encrypt an existing archive (including file names):",
      "syntax": "7zr a {{path/to/encrypted.7z}} -p{{password}} -mhe={{on}} {{path/to/archive.7z}}"
    },
    {
      "description": "E[x]tract an archive preserving the original directory structure:",
      "syntax": "7zr x {{path/to/archive.7z}}"
    },
    {
      "description": "E[x]tract an archive to a specific directory:",
      "syntax": "7zr x {{path/to/archive.7z}} -o{{path/to/output}}"
    },
    {
      "description": "E[x]tract an archive to stdout:",
      "syntax": "7zr x {{path/to/archive.7z}} -so"
    },
    {
      "description": "[l]ist the contents of an archive:",
      "syntax": "7zr l {{path/to/archive.7z}}"
    },
    {
      "description": "Set the level of compression (higher means more compression, but slower):",
      "syntax": "7zr a {{path/to/archive.7z}} -mx={{0|1|3|5|7|9}} {{path/to/file_or_directory}}"
    }
  ]
}