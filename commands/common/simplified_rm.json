{
  "command": "rm",
  "description": "Remove files or directories. See also: rmdir. More information: https://www.gnu.org/software/coreutils/manual/html_node/rm-invocation.html.",
  "useCases": [
    {
      "description": "Remove specific files:",
      "syntax": "rm {{path/to/file1 path/to/file2 ...}}"
    },
    {
      "description": "Remove specific files ignoring nonexistent ones:",
      "syntax": "rm -f {{path/to/file1 path/to/file2 ...}}"
    },
    {
      "description": "Remove specific files interactively prompting before each removal:",
      "syntax": "rm -i {{path/to/file1 path/to/file2 ...}}"
    },
    {
      "description": "Remove specific files printing info about each removal:",
      "syntax": "rm -v {{path/to/file1 path/to/file2 ...}}"
    },
    {
      "description": "Remove specific files and directories recursively:",
      "syntax": "rm -r {{path/to/file_or_directory1 path/to/file_or_directory2 ...}}"
    }
  ]
}