{
  "command": "netdiscover",
  "description": "Network scanner used to find live hosts on a network. More information: https://github.com/netdiscover-scanner/netdiscover.",
  "useCases": [
    {
      "description": "Scan the IP range on the network interface for active hosts:",
      "syntax": "netdiscover -r {{172.16.6.0/23}} -i {{ens244}}"
    }
  ]
}