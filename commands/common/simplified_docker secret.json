{
  "command": "docker secret",
  "description": "Manage Docker swarm secrets. More information: https://docs.docker.com/reference/cli/docker/secret/.",
  "useCases": [
    {
      "description": "Create a new secret from stdin:",
      "syntax": "{{command}} | docker secret create {{secret_name}} -"
    },
    {
      "description": "Create a new secret from a file:",
      "syntax": "docker secret create {{secret_name}} {{path/to/file}}"
    },
    {
      "description": "List all secrets:",
      "syntax": "docker secret ls"
    },
    {
      "description": "Display detailed information on one or multiple secrets in a human friendly format:",
      "syntax": "docker secret inspect --pretty {{secret_name1 secret_name2 ...}}"
    },
    {
      "description": "Remove one or more secrets:",
      "syntax": "docker secret rm {{secret_name1 secret_name2 ...}}"
    }
  ]
}