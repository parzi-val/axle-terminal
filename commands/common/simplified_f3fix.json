{
  "command": "f3fix",
  "description": "Edit the partition table of a fake flash drive. See also: f3probe, f3write, f3read. More information: https://oss.digirati.com.br/f3/.",
  "useCases": [
    {
      "description": "Fill a fake flash drive with a single partition that matches its real capacity:",
      "syntax": "sudo f3fix {{/dev/device_name}}"
    },
    {
      "description": "Mark the partition as bootable:",
      "syntax": "sudo f3fix --boot {{/dev/device_name}}"
    },
    {
      "description": "Specify the filesystem:",
      "syntax": "sudo f3fix --fs-type={{filesystem_type}} {{/dev/device_name}}"
    }
  ]
}