{
  "command": "spfquery",
  "description": "Query Sender Policy Framework records to validate e-mail senders. More information: https://manned.org/spfquery.",
  "useCases": [
    {
      "description": "Check if an IP address is allowed to send an e-mail from the specified e-mail address:",
      "syntax": "spfquery -ip {{8.8.8.8}} -sender {{sender@example.com}}"
    },
    {
      "description": "Turn on debugging output:",
      "syntax": "spfquery -ip {{8.8.8.8}} -sender {{sender@example.com}} --debug"
    }
  ]
}