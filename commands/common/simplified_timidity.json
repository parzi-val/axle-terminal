{
  "command": "timidity",
  "description": "Play and convert MIDI files. More information: https://timidity.sourceforge.net.",
  "useCases": [
    {
      "description": "Play a MIDI file:",
      "syntax": "timidity {{path/to/file.mid}}"
    },
    {
      "description": "Play a MIDI file in a loop:",
      "syntax": "timidity --loop {{path/to/file.mid}}"
    },
    {
      "description": "Play a MIDI file in a specific key (0 = C major/A minor, -1 = F major/D minor, +1 = G major/E minor, etc.):",
      "syntax": "timidity --force-keysig={{-flats|+sharps}} {{path/to/file.mid}}"
    },
    {
      "description": "Convert a MIDI file to PCM (WAV) audio:",
      "syntax": "timidity --output-mode={{w}} --output-file={{path/to/file.wav}} {{path/to/file.mid}}"
    },
    {
      "description": "Convert a MIDI file to FLAC audio:",
      "syntax": "timidity --output-mode={{F}} --output-file={{path/to/file.flac}} {{path/to/file.mid}}"
    }
  ]
}