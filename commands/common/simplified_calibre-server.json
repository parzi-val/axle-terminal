{
  "command": "calibre-server",
  "description": "A server application to distribute e-books over a network. Note: e-books must already be imported into the library using the GUI or the calibredb CLI. Part of the Calibre e-book library. More information: https://manual.calibre-ebook.com/generated/en/calibre-server.html.",
  "useCases": [
    {
      "description": "Start a server to distribute e-books. Access at http://localhost:8080:",
      "syntax": "calibre-server"
    },
    {
      "description": "Start server on different port. Access at http://localhost:port:",
      "syntax": "calibre-server --port {{port}}"
    },
    {
      "description": "Password protect the server:",
      "syntax": "calibre-server --username {{username}} --password {{password}}"
    }
  ]
}