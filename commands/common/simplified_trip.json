{
  "command": "trip",
  "description": "A network diagnostic tool. Combines the functionality of traceroute and ping. Designed to assist with the analysis of networking issues. More information: https://trippy.rs/.",
  "useCases": [
    {
      "description": "Basic usage with default parameters:",
      "syntax": "sudo trip {{example.com}}"
    },
    {
      "description": "Trace without requiring elevated privileges (supported platforms only):",
      "syntax": "trip {{example.com}} --unprivileged"
    },
    {
      "description": "Trace using IPv6 only:",
      "syntax": "sudo trip {{example.com}} --ipv6"
    },
    {
      "description": "Trace using the udp protocol:",
      "syntax": "sudo trip {{example.com}} --protocol {{udp}}"
    },
    {
      "description": "Use custom destination port 443 for tcp tracing:",
      "syntax": "sudo trip {{example.com}} --protocol {{tcp}} --target-port {{443}}"
    },
    {
      "description": "Use custom source port 5000 for udp tracing:",
      "syntax": "sudo trip {{example.com}} --protocol {{udp}} --source-port {{5000}}"
    }
  ]
}