{
  "command": "flex",
  "description": "Lexical analyzer generator. A rewrite of lex with extensions to the POSIX specification. Given the specification for a lexical analyzer, generates C code implementing it. Note: long options don't work on OpenBSD. More information: https://manned.org/flex.",
  "useCases": [
    {
      "description": "Generate an analyzer from a flex file, storing it to the file lex.yy.c:",
      "syntax": "lex {{analyzer.l}}"
    },
    {
      "description": "Write analyzer to stdout:",
      "syntax": "lex -{{-stdout|t}} {{analyzer.l}}"
    },
    {
      "description": "Specify the output file:",
      "syntax": "lex {{analyzer.l}} -o {{analyzer.c}}"
    },
    {
      "description": "Generate a [B]atch scanner instead of an interactive scanner:",
      "syntax": "lex -B {{analyzer.l}}"
    },
    {
      "description": "Compile a C file generated by Lex:",
      "syntax": "cc {{path/to/lex.yy.c}} --output {{executable}}"
    }
  ]
}