{
  "command": "ya",
  "description": "Manage Yazi packages and plugins. More information: https://github.com/sxyazi/yazi.",
  "useCases": [
    {
      "description": "Add a package:",
      "syntax": "ya pack -a {{package}}"
    },
    {
      "description": "Upgrade all packages:",
      "syntax": "ya pack -u"
    },
    {
      "description": "Subscribe to messages from all remote instances:",
      "syntax": "ya sub {{kinds}}"
    },
    {
      "description": "Publish a message to the current instance with string body:",
      "syntax": "ya pub --str {{string_message}}"
    },
    {
      "description": "Publish a message to the current instance with JSON body:",
      "syntax": "ya pub --json {{json_message}}"
    },
    {
      "description": "Publish a message to the specified instance with string body:",
      "syntax": "ya pub-to --str {{message}} {{receiver}} {{kind}}"
    }
  ]
}