{
  "command": "pbmtonokia",
  "description": "Convert a PBM image to one of Nokia's Smart Messaging Formats . More information: https://netpbm.sourceforge.net/doc/pbmtonokia.html.",
  "useCases": [
    {
      "description": "Convert a PBM image into a Nokia Operator Logo as hexcode:",
      "syntax": "pbmtonokia -fmt NEX_NOL -net {{network_operator_code}} {{path/to/image.pbm}} &gt; {{path/to/output.hex}}"
    },
    {
      "description": "Convert a PBM image into a Nokia Group Graphic as hexcode:",
      "syntax": "pbmtonokia -fmt NEX_NGG {{path/to/image.pbm}} &gt; {{path/to/output.hex}}"
    },
    {
      "description": "Convert a PBM image into a Nokia Picture Message with the specified text as hexcode:",
      "syntax": "pbmtonokia -fmt NEX_NPM -txt {{text_message}} {{path/to/image.pbm}} &gt; {{path/to/output.hex}}"
    },
    {
      "description": "Convert a PBM image into a Nokia Operator Logo as a NOL file:",
      "syntax": "pbmtonokia -fmt NOL {{path/to/image.pbm}} &gt; {{path/to/output.nol}}"
    },
    {
      "description": "Convert a PBM image into a Nokia Group Graphic as an NGG file:",
      "syntax": "pbmtonokia -fmt NGG {{path/to/image.pbm}} &gt; {{path/to/output.ngg}}"
    },
    {
      "description": "Convert a PBM image into a Nokia Picture Message as an NPM file:",
      "syntax": "pbmtonokia -fmt NPM {{path/to/image.pbm}} &gt; {{path/to/output.npm}}"
    }
  ]
}