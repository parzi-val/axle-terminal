{
  "command": "sdiff",
  "description": "Compare the differences between and optionally merge 2 files. More information: https://manned.org/sdiff.",
  "useCases": [
    {
      "description": "Compare 2 files:",
      "syntax": "sdiff {{path/to/file1}} {{path/to/file2}}"
    },
    {
      "description": "Compare 2 files, ignoring all tabs and whitespace:",
      "syntax": "sdiff -W {{path/to/file1}} {{path/to/file2}}"
    },
    {
      "description": "Compare 2 files, ignoring whitespace at the end of lines:",
      "syntax": "sdiff -Z {{path/to/file1}} {{path/to/file2}}"
    },
    {
      "description": "Compare 2 files in a case-insensitive manner:",
      "syntax": "sdiff -i {{path/to/file1}} {{path/to/file2}}"
    },
    {
      "description": "Compare and then merge, writing the output to a new file:",
      "syntax": "sdiff -o {{path/to/merged_file}} {{path/to/file1}} {{path/to/file2}}"
    }
  ]
}