{
  "command": "ncc",
  "description": "Compile a Node.js application into a single file. Supports TypeScript, binary addons and dynamic requires. More information: https://github.com/vercel/ncc.",
  "useCases": [
    {
      "description": "Bundle a Node.js application:",
      "syntax": "ncc build {{path/to/file.js}}"
    },
    {
      "description": "Bundle and minify a Node.js application:",
      "syntax": "ncc build --minify {{path/to/file.js}}"
    },
    {
      "description": "Bundle and minify a Node.js application and generate source maps:",
      "syntax": "ncc build --source-map {{path/to/file.js}}"
    },
    {
      "description": "Automatically recompile on changes to source files:",
      "syntax": "ncc build --watch {{path/to/file.js}}"
    },
    {
      "description": "Bundle a Node.js application into a temporary directory and run it for testing:",
      "syntax": "ncc run {{path/to/file.js}}"
    },
    {
      "description": "Clean the ncc cache:",
      "syntax": "ncc clean cache"
    }
  ]
}