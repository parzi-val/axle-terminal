{
  "command": "kubectl taint",
  "description": "Update the taints on nodes. More information: https://kubernetes.io/docs/reference/generated/kubectl/kubectl-commands#taint.",
  "useCases": [
    {
      "description": "Apply taint to a node:",
      "syntax": "kubectl taint nodes {{node_name}} {{label_key}}={{label_value}}:{{effect}}"
    },
    {
      "description": "Remove taint from a node:",
      "syntax": "kubectl taint nodes {{node_name}} {{label_key}}:{{effect}}-"
    },
    {
      "description": "Remove all taints from a node:",
      "syntax": "kubectl taint nodes {{node_name}} {{label_key}}-"
    }
  ]
}