{
  "command": "az storage",
  "description": "Manage Azure Cloud Storage resources. Part of azure-cli (also known as az). More information: https://learn.microsoft.com/cli/azure/storage.",
  "useCases": [
    {
      "description": "Create a storage account specifying a [l]ocation:",
      "syntax": "az storage account create --resource-group {{group_name}} --name {{account_name}} -l {{location}} --sku {{account_sku}}"
    },
    {
      "description": "List all storage accounts in a resource group:",
      "syntax": "az storage account list --resource-group {{group_name}}"
    },
    {
      "description": "List the access keys for a storage account:",
      "syntax": "az storage account keys list --resource-group {{group_name}} --name {{account_name}}"
    },
    {
      "description": "Delete a storage account:",
      "syntax": "az storage account delete --resource-group {{group_name}} --name {{account_name}}"
    },
    {
      "description": "Update the minimum tls version setting for a storage account:",
      "syntax": "az storage account update --min-tls-version {{TLS1_0|TLS1_1|TLS1_2}} --resource-group {{group_name}} --name {{account_name}}"
    }
  ]
}