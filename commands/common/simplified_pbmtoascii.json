{
  "command": "pbmtoascii",
  "description": "Convert a PBM image to ASCII graphics. See also: ppmtoascii, asciitopgm, ppmtoterm. More information: https://netpbm.sourceforge.net/doc/pbmtoascii.html.",
  "useCases": [
    {
      "description": "Read a PBM file as input and produce an ASCII output:",
      "syntax": "pbmtoascii {{path/to/input_file.pbm}}"
    },
    {
      "description": "Read a PBM file as input and save an ASCII output into a file:",
      "syntax": "pbmtoascii {{path/to/input_file.pbm}} &gt; {{path/to/output_file}}"
    },
    {
      "description": "Read a PBM file as input while setting the pixel mapping (defaults to 1x2):",
      "syntax": "pbmtoascii -{{1x2|2x4}} {{path/to/input_file.pbm}}"
    },
    {
      "description": "Display version:",
      "syntax": "pbmtoascii -version"
    }
  ]
}