{
  "command": "dbx",
  "description": "Interact with the Databricks platform. Note: this tool has been retired and it is recommended to use Databricks Asset Bundles instead. More information: https://dbx.readthedocs.io/en/latest/reference/cli/#dbx.",
  "useCases": [
    {
      "description": "Create a new dbx project in the current working directory:",
      "syntax": "dbx configure --profile {{DEFAULT}}"
    },
    {
      "description": "Sync local files from the specified path to DBFS and watch for changes:",
      "syntax": "dbx sync dbfs --source {{path/to/directory}} --dest {{path/to/remote_directory}}"
    },
    {
      "description": "Deploy the specified workflow to artifact storage:",
      "syntax": "dbx deploy {{workflow_name}}"
    },
    {
      "description": "Launch the specified workflow after deploying it:",
      "syntax": "dbx launch {{workflow_name}}"
    }
  ]
}