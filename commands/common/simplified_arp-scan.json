{
  "command": "arp-scan",
  "description": "Send ARP packets to hosts (specified as IP addresses or hostnames) to scan the local network. More information: https://github.com/royhills/arp-scan.",
  "useCases": [
    {
      "description": "Scan the current local network:",
      "syntax": "arp-scan --localnet"
    },
    {
      "description": "Scan an IP network with a custom bitmask:",
      "syntax": "arp-scan {{192.168.1.1}}/{{24}}"
    },
    {
      "description": "Scan an IP network within a custom range:",
      "syntax": "arp-scan {{127.0.0.0}}-{{127.0.0.31}}"
    },
    {
      "description": "Scan an IP network with a custom net mask:",
      "syntax": "arp-scan {{10.0.0.0}}:{{255.255.255.0}}"
    }
  ]
}