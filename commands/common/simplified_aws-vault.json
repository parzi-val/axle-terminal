{
  "command": "aws-vault",
  "description": "A vault for securely storing and accessing AWS credentials in development environments. More information: https://github.com/99designs/aws-vault.",
  "useCases": [
    {
      "description": "Add credentials to the secure keystore:",
      "syntax": "aws-vault add {{profile}}"
    },
    {
      "description": "Execute a command with AWS credentials in the environment:",
      "syntax": "aws-vault exec {{profile}} -- {{aws s3 ls}}"
    },
    {
      "description": "Open a browser window and login to the AWS Console:",
      "syntax": "aws-vault login {{profile}}"
    },
    {
      "description": "List profiles, along with their credentials and sessions:",
      "syntax": "aws-vault list"
    },
    {
      "description": "Rotate AWS credentials:",
      "syntax": "aws-vault rotate {{profile}}"
    },
    {
      "description": "Remove credentials from the secure keystore:",
      "syntax": "aws-vault remove {{profile}}"
    }
  ]
}